# Feature 232 — lpalbou-AbstractGateway-mit-replay-first-durable-ai-control-plane-http-telegram-persistence-append-only-ledger-v2-ai-durable-control-plane (defer)

> **NEW observation (2026-09-26) + LICENSE UPGRADE `null → MIT`.** Documents in-window GitHub Search `append-only+ledger` query result + watchlist direct-GET: `lpalbou/AbstractGateway` (**MIT ✓ §3.2 STRICTLY-COMPATIBLE** — was `null` on 2026-09-23 per carry-over observation, **LICENSE UPGRADE observed between 2026-09-23 and 2026-09-26**; this unblocks the §3.2 gate that previously prevented feature filing; parent-direct-GET confirms verbatim license object: `{"key": "mit", "name": "MIT License", "spdx_id": "MIT", ...}`), **4★/0⑂ = third-highest-star-count in-window candidate of the 57-pass daily-research series** (parent-direct-GET confirms; subagent's search-result JSON scored stars=`None`), Python, **pushed 2026-09-25T01:41:25Z** (in-window by `pushed_at` only — *1 day before fetch time*), **created 2026-01-07T20:45:36Z** (~8.5-month-old repo with in-window `pushed_at`; **IN-WINDOW BY PUSH ONLY**; `created_at` is OUT-OF-WINDOW by ~8.5 months), **11539 KB** substantial repo, `default_branch=main`, archived=`False`). **12 topics** (verbatim from raw JSON): `abstractcore`, `abstractframework`, `abstraction`, `agent`, `ai`, `api`, `durable`, `gateway`, `llm`, `memory`, `persistence`, `runtime`. Description (verbatim, parent-verified GitHub API direct-GET 2026-09-26): *"The production HTTP control plane for durable AI runs. Start, schedule, observe, and replay AI workflows across any client — terminal, browser, tray, Telegram, or email. Replay-first architecture with append-only ledger."* The **replay-first + durable + HTTP control plane + multi-client (terminal + browser + tray + Telegram + email) + append-only-ledger + memory** septuple-primitive + the *production HTTP control plane for durable AI runs* discipline = the **strongest single v2-AI durable-AI-control-plane vocabulary of the 57-pass series**. Bucket: **v2-AI durable-AI-control-plane (parking-lot, future-v2-AI-surface-vocabulary-reference)** — watch-list entry, zero build time today. **CRITICAL OBSERVATION**: the license upgrade `null → MIT` unblocks §3.2 for any future v2-AI adoption of the AbstractGateway vocabulary; this is the most actionable single finding of today's pass.

## Goal

Retain the **replay-first + durable + HTTP control plane + multi-client + append-only-ledger + memory** septuple-primitive as a persistent cross-section reference for any future LE31 v2-AI surface that introduces a durable AI workflow with (a) **HTTP control plane** (the AI workflow is orchestrated via HTTP), (b) **durable execution** (the AI workflow survives crashes + restarts + operator absence), (c) **multi-client access** (the operator can start/observe/replay the workflow from terminal + browser + tray + Telegram + email), (d) **replay-first architecture** (the workflow's history is the source of truth for replay + audit), and (e) **append-only ledger** (the workflow's history is append-only + hash-linked). The artifact is the persistent cross-section reference + the 12 named topics + the verbatim description. No code today (MIT license permits future code reuse; vocabulary-only artifact today; 11539 KB substantial-repo size is substantial but readable; the *replay-first architecture + durable + HTTP control plane + multi-client + append-only-ledger + memory* vocabulary is the load-bearing value).

## Scope

**In scope (defer artifact):**
- A written record of the **replay-first architecture** discipline: the *workflow-history-as-source-of-truth* primitive (LE31 v1 today has `audit_logs` for state transitions but no notion of *replay* of the entire workflow from a single canonical history; the *replay-first architecture* discipline IS the *workflow-history-enables-replay* posture applied to charter §3.1's *explicit-state-transition* discipline).
- A written record of the **durable execution** discipline: the *workflow-survives-crashes* primitive (LE31 v1 today has Postgres-backed `audit_logs` but no explicit *durable execution* primitive that survives operator absence + crash + restart; the *durable execution* discipline IS the *workflow-durability* posture).
- A written record of the **HTTP control plane** discipline: the *HTTP-orchestrated-AI-workflow* primitive (the AI workflow is orchestrated via HTTP, not via in-process calls; LE31 v1 today uses in-process Python for all orchestration; the *HTTP control plane* vocabulary is a v2-AI future reference for any AI workflow that needs to be orchestrated across processes / clients / restarts).
- A written record of the **multi-client access** discipline: the *multi-surface-workflow-access* primitive (the workflow is accessible from terminal + browser + tray + Telegram + email; LE31 v1 today has Telegram access for the cook-bot but no terminal/browser/tray/email access; the *multi-client* vocabulary is a v2-AI future reference).
- A written record of the **append-only-ledger** discipline: the *workflow-history-append-only* primitive (the workflow's history is append-only + hash-linked; LE31 v1's `audit_logs` is append-only but does NOT hash-link; the *append-only-ledger* vocabulary is a v2-AI future reference).
- A written record of the **memory + persistence** discipline: the *workflow-state-persistence* primitive (the workflow's state is persisted across runs; LE31 v1 today has Postgres-backed state for `audit_logs` + `StockEntry` but no explicit *workflow-state-persistence* primitive that survives across runs).
- A decision record: today's verdict is `defer` because LE31 v1 has no AI surface at all (charter §3.4 explicit invariant); the cross-section reference is informative, not a v2-AI build-trigger.
- A cross-section reference with the prior v2-AI append-only-ledger + AI-runtime + HTTP-control-plane cluster: features 149 (`personal-agent-blueprint-telegram-chokepoint-architecture-pattern` carry-over from 09-12; same Telegram-chokepoint vocabulary), 187 (`traust-ledger` Apache-2.0 2★/5⑂ carry-over from 09-17; same append-only-ledger vocabulary), 198 (`arbiter` MIT 0★ carry-over from 09-19; same SEC Form 4/8-K ingestion vocabulary), 220 (`commit-replay-bench` Apache-2.0 0★ carry-over from 09-23; same held-out-tests vocabulary), 230 (`mentu-ai/commitment-protocol` MIT 10★/4⑂ = today's Pick A = same commitment-ledger vocabulary cluster), 231 (`LinkedParticles/particles-standard` Apache-2.0 7★/0⑂ = today's Pick B = same sourced-claim-ledger vocabulary cluster). The *transferable insight* is the **replay-first + durable + HTTP control plane + multi-client + append-only-ledger + memory** septuple-primitive.

**Out of scope (defer artifact):**
- Any change to LE31 v1's data model.
- Any change to the waiter web UI.
- Any change to the cook Telegram bot.
- Any change to the `audit_logs` schema.
- Any change to the `StockEntry` schema.
- Any v2 surface in v1 (charter §3.1 + §3.2 invariant: v1 surface expansion is the next boundary; cross-section is informative, not a v2 expansion trigger).
- Any customer-facing AI surface in v1 (charter §3.4 explicit invariant: NO customer-facing AI).
- Any owner-facing AI surface in v1 (LE31 v1 has no AI surface at all).
- Any v2 horizontal-expansion surface in v2 (charter §3.1 + §3.2 invariant: v2 surface expansion is the next boundary; this is a vocabulary reference, not a v2 expansion trigger).
- Any v2-AI durable-AI-control-plane implementation in v2 (the artifact is vocabulary-only; the implementation would require explicit owner/charter sign-off).
- Any HTTP-control-plane primitive in v1 (LE31 v1 today uses in-process Python for all orchestration; the *HTTP control plane* vocabulary is a v2-AI future reference).
- Any replay-first architecture in v1 (LE31 v1 has no replay-first surface today; the *replay-first architecture* vocabulary is a v2-AI future reference).
- Any multi-client (terminal + browser + tray + Telegram + email) workflow access in v1 (LE31 v1 has only Telegram access for the cook-bot; the *multi-client* vocabulary is a v2-AI future reference).

## Description

The pick is **`lpalbou/AbstractGateway`** — a Python + MIT (license upgraded from `null` between 09-23 and 09-26) + replay-first + durable + HTTP-control-plane + multi-client + append-only-ledger + memory for AI workflows. Charter §3.4 NOT triggered because the gateway itself is operator-tooling-AI (the gateway orchestrates the AI workflow; the operator/owner is the consumer of the orchestration); **HOWEVER**, the *multi-client access* including Telegram as a client surface could potentially be customer-facing if the Telegram client is exposed to restaurant diners; this requires §3.4 review for any future v2-AI surface that adopts the AbstractGateway vocabulary. The *durable-AI-control-plane* is a tool for the *operator/owner* who supervises the AI; charter §3.4 is satisfied for operator-tooling surfaces, with the **caveat** that any Telegram-client adoption requires explicit §3.4 review. The artifact is the persistent cross-section reference for the 12 named topics + the verbatim description + the 7 named primitives + the LICENSE UPGRADE observation.

## Data model

No data model changes today. The artifact is a vocabulary reference, not a code change. Future v2-AI surface that adopts the *durable-AI-control-plane* primitive would extend the LE31 v1 data model with appropriate new tables (e.g., a `workflow_run` table with `run_id, workflow_type, started_at, completed_at, status, client_surface, replay_history_id`; a `workflow_replay_history` table with `history_id, run_id, recorded_at, state_snapshot, hash_chain_link`; a `workflow_client` table with `client_id, client_type, client_uri, last_seen_at, authenticated`). All future schema extensions are explicitly out-of-scope for this defer artifact.

## Implementation steps

Zero implementation today (defer artifact). If a future v2-AI PR is triggered by the trigger condition below, the implementation would:
1. Read the `lpalbou/AbstractGateway` README at https://github.com/lpalbou/AbstractGateway for the *replay-first + durable + HTTP control plane + multi-client + append-only-ledger + memory* septuple-primitive.
2. Cross-reference with LE31 v1's `audit_logs` schema to identify the *delta* (the *delta* = `lpalbou/AbstractGateway` introduces replay-first + durable + HTTP-control-plane + multi-client + append-only-ledger + memory primitives that LE31 v1's `audit_logs` does not have; LE31 v1's `audit_logs` is append-only but does NOT hash-link, does NOT have replay-first architecture, does NOT have HTTP control plane, does NOT have multi-client access, does NOT have explicit *workflow-run* records).
3. Apply charter §3.4 *operator-tooling-AI with observable evidence + non-AI fallback* review to the *delta* (any new v2-AI surface requires explicit owner/charter sign-off; the *durable-AI-control-plane* surface is operator-tooling-AI which is charter §3.4-compatible provided the non-AI fallback is preserved — i.e., the operator can view and approve every workflow-run + replay without using the AI to interpret it; **the Telegram-client surface requires §3.4 review** because Telegram could be customer-facing if exposed to restaurant diners).
4. Implement the surface with the LE31 v1 + FastAPI + SQLModel + aiogram stack; the `lpalbou/AbstractGateway` reference is the *vocabulary* for *replay-first + durable + HTTP control plane + multi-client + append-only-ledger + memory*, NOT for *durable-AI-control-plane replacement*.

## Telegram interaction

Zero new Telegram interaction today. Future v2-AI PR that adopts the *durable-AI-control-plane* primitive would extend the existing aiogram-bot (cook-bot per charter §3.1) with a *workflow-status* command set that allows the operator to query workflow-run status (e.g., `/workflow list` → list of recent workflow-runs with status + client surface; `/workflow show <run_id>` → show full workflow-run details + replay history; `/workflow replay <run_id>` → trigger a replay of a past workflow-run). The *workflow-status* discipline is *operator-tooling* (operator queries the workflow-run status), NOT customer-facing-AI; charter §3.4 is NOT triggered. **HOWEVER**, exposing the same workflow-status to restaurant diners via the Telegram client would require explicit §3.4 review because diners-as-AI-consumers is a §3.4 hard-block.

## Dependencies

- LE31 charter §3.1 surface-expansion review (for any v2 surface adoption).
- LE31 charter §3.2 surface-expansion review (for any v2-AI surface adoption; **the LICENSE UPGRADE `null → MIT` unblocks this review**).
- LE31 charter §3.4 operator-tooling-AI-with-observable-evidence boundary (for any durable-AI-control-plane adoption; the *durable-AI-control-plane* posture is operator-tooling-AI which is charter §3.4-compatible provided the non-AI fallback is preserved; **Telegram-client exposure requires explicit §3.4 review**).
- LE31 charter §3.2 license-compatible (MIT permissive as of 2026-09-26; future code adoption is possible; was `null` on 09-23 = upgrade observed).
- Features 149 (`personal-agent-blueprint-telegram-chokepoint-architecture-pattern`), 187 (`traust-ledger`), 198 (`arbiter`), 220 (`commit-replay-bench`), 230 (`mentu-ai/commitment-protocol` = today's Pick A), 231 (`LinkedParticles/particles-standard` = today's Pick B).
- Cross-section reference `lpalbou/AbstractGateway` at https://github.com/lpalbou/AbstractGateway (MIT as of 2026-09-26, was `null` on 09-23, 4★/0⑂, Python, replay-first + durable + HTTP control plane + multi-client + append-only-ledger + memory).

## Open questions

- Will LE31 v2-AI ever introduce a durable-AI-control-plane surface? If yes, `lpalbou/AbstractGateway` is the vocabulary reference (now that the license is MIT).
- Will LE31 v2-AI ever introduce a replay-first architecture? If yes, `lpalbou/AbstractGateway` is the vocabulary reference.
- Will LE31 v2-AI ever introduce an HTTP-control-plane primitive? If yes, `lpalbou/AbstractGateway` is the vocabulary reference.
- Will LE31 v2-AI ever introduce a multi-client (terminal + browser + tray + Telegram + email) workflow access? If yes, `lpalbou/AbstractGateway` is the vocabulary reference (**with §3.4 review for Telegram-client exposure**).
- Will LE31 v2-AI ever introduce a hash-chained append-only-ledger? If yes, `lpalbou/AbstractGateway` is the vocabulary reference.
- The 4★/0⑂ is explicitly NOT offered as evidence of *LE31 needing a durable-AI-control-plane*; the 4★/0⑂ is for the *durable-AI-control-plane domain*, not for the *LE31 operational* primitive. The transferable item is the *technique* (replay-first + durable + HTTP control plane + multi-client + append-only-ledger + memory), not the *durable-AI-control-plane* itself.
- **Open question that may kill any future PR**: who would ever run the offline replay verifier of a workflow-run history? LE31 has exactly one stakeholder (the owner) who can just ask the AI what it did. The *replay-first architecture* may be over-engineering for a single-tenant single-operator scenario.
- **CRITICAL LICENSE UPGRADE QUESTION**: when exactly did the `lpalbou/AbstractGateway` LICENSE change from `null` to `MIT`? The 09-23 observation recorded `null`; the 09-25 record (per subagent) recorded `null`; today's 09-26 record recorded `MIT`. The upgrade must have occurred between 2026-09-25T01:41:25Z (last push before license change) and the parent re-fetch today (2026-09-26). Recommend a manual LICENSE file inspection to confirm the upgrade is permanent (not a GitHub API metadata fluctuation).

## Why this matters

The 12 topics + the verbatim description + the LICENSE UPGRADE `null → MIT` in `lpalbou/AbstractGateway` are the **canonical v2-AI durable-AI-control-plane vocabulary** that LE31 v1's `audit_logs` (feature/charter §3.1) and v2-AI's future AI-workflow surface would inherit. Specifically: (i) **replay-first** (the *workflow-history-as-source-of-truth* primitive) is the *workflow-history-enables-replay* posture; (ii) **durable** (the *workflow-survives-crashes* primitive) is the *workflow-durability* posture; (iii) **HTTP control plane** (the *HTTP-orchestrated-AI-workflow* primitive) is the *workflow-HTTP-orchestration* posture; (iv) **multi-client** (the *multi-surface-workflow-access* primitive) is the *terminal-browser-tray-Telegram-email-workflow-access* posture; (v) **append-only-ledger** (the *workflow-history-append-only* primitive) is the *workflow-history-hash-linked* posture per charter §3.4's *observable-evidence* requirement; (vi) **memory + persistence** (the *workflow-state-persistence* primitive) is the *workflow-state-survives-runs* posture; (vii) **LICENSE UPGRADE `null → MIT`** is the *§3.2-unblocking-event* that enables any future v2-AI adoption. When v2-AI introduces any of these 7 primitives, `lpalbou/AbstractGateway` is the vocabulary reference. **HONEST DISCLOSURE**: `lpalbou/AbstractGateway`'s 4★/0⑂ is for the *durable-AI-control-plane domain*, not for the *LE31 operational* primitive; the 4★/0⑂ is explicitly NOT offered as evidence of LE31's need for the durable-AI-control-plane. **CRITICAL OBSERVATION**: the LICENSE UPGRADE `null → MIT` between 09-23 and 09-26 is the most actionable single finding of today's pass; the §3.2 gate that previously blocked feature filing is now lifted.