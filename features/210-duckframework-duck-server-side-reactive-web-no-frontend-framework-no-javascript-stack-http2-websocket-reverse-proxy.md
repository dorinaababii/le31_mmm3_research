# Feature 210 — `duckframework-duck-server-side-reactive-web-no-frontend-framework-no-javascript-stack-http2-websocket-reverse-proxy` (defer)

> **NEW observation (2026-09-21).** Documents in-window GitHub Search `topic:real-time+language:python` query result: `duckframework/duck` (**46★/3⑂**, Python, **pushed 2026-09-17T17:55:19Z = 4 days ago** (in-window by `pushed_at` only — *created 2022-12-04T11:54:27Z = OUT-OF-WINDOW, ~3.8 years before the 2026-08-22 window-start*), **20529 KB substantial repo** (the **largest in-window 2026 Python server-side-reactive-web repo of the 53-pass brainstorm series**; vs the *62089 KB* `djust-org/djust` from feature 206 = 3.0× smaller; vs the *9471 KB* `lbliii/chirp` from feature 54 = 2.2× larger), default_branch=`main`, archived=`False`). **20 topics** (verbatim from raw JSON): `async`, `backend`, `developer-tools`, `full-stack`, `high-performance`, `http2`, `lively-ui`, `modern-web`, `open-source`, `python3`, `reactive`, `reactive-ui`, `real-time`, `reverse-proxy`, `scalable`, `server-side`, `web-development`, `web-framework`, `web-server`, `websocket`. Description (verbatim, parent-verified GitHub API direct-GET 2026-09-21): *"Build high-performance, scalable, server-side reactive web applications — without a separate frontend framework or a complex JavaScript stack."* The **server-side-reactive-web + no-frontend-framework + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time** octuple primitive — the *server-driven reactivity* pattern where the *server* renders the DOM + *streams DOM-diffs over WebSocket* + *no separate React/Vue/Svelte build step is required*. The phrase *"server-side reactive web applications"* explicitly names the *server-driven reactivity* pattern. The phrase *"without a separate frontend framework or a complex JavaScript stack"* explicitly names the *no-SPA + no-JS-build* posture — when a web app is *server-side reactive*, the *no-build-step* discipline is the *pure-server-rendering + minimal-client-JS* posture that LE31 v1's HTMX stack already implements (HTMX = pure-HTML + tiny-JS-defer-library). The topics `reactive + reactive-ui + server-side + websocket + real-time + http2` are the *server-side-reactive-UI-over-WebSocket + HTTP/2-multiplexing* sextuple. The topic `reverse-proxy` names the *reverse-proxy-server* primitive (duckframework/duck includes a built-in reverse-proxy; LE31 v1's FastAPI + uvicorn does not include a reverse-proxy; the *reverse-proxy-as-application-server* primitive is a future v2 architectural option for any v2 surface that needs to terminate WebSocket connections directly). The **46★ community adoption signal** is meaningful — 46 stars means a real community uses the *server-side-reactive-web + no-SPA + no-JS-build* primitive at scale. The **BSD-3-Clause license is §3.2 STRICTLY-COMPATIBLE** for vocabulary + architecture-reference adoption (BSD-3-Clause = permissive license compatible with the LE31 charter §3.2 stack-invariant posture). Charter §3.1 alignment (the *server-side-reactive-web* discipline is the *stateful-connection + server-pushed-DOM-diffs* pattern applied to charter §3.1's *explicit-state-transition* discipline — the server is the source of truth; the client receives DOM-diffs); §3.2 STRICTLY-COMPATIBLE (BSD-3-Clause permissive); §3.4 not triggered (no AI surface; the *server-side-reactive-web* pattern is *operator-tooling UI* territory). Sister-shape to features 23 (`sse-cook-channel` primitive) + 25 (`fastapi-frontend-dev-loop` primitive) + 54 (`corner-mart POS reference` + `lbliii/chirp` adjacent evidence) + 142 (`feldroy-air-fastapi-htmx-ai-write-framework`) + 206 (`djust-org/djust` Phoenix-LiveView-style reactive-SSR primitive) via the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* octuple primitive. **Fully reversible** (vocabulary-only artifact).

## Goal

Retain the **server-side-reactive-web + no-frontend-framework + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time** cross-section architectural vocabulary + **the *server-driven reactivity* pattern** + **the *no-SPA + no-JS-build* discipline** + **the *HTTP/2-multiplexed-WebSocket + reverse-proxy-server* primitives** as the persistent v2 no-build reactive-web reference for any future LE31 v2 maintainer asking the *no-build + no-SPA + reactive-UI-over-WebSocket* question (does LE31 v2 ever need a server-side-reactive-web surface? does LE31 v2 ever need a WebSocket-stateful-connection primitive? does LE31 v2 ever need an HTTP/2-multiplexed-websocket primitive? does LE31 v2 ever need a reverse-proxy-as-application-server primitive?). The artifact is the persistent *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* vocabulary as a *named* v2 no-build reactive-web reference. No code today.

## Scope

**In scope (defer artifact):**
- A written record of the `duckframework/duck` cross-section vocabulary: the **largest in-window 2026 Python server-side-reactive-web repo of the 53-pass series** with the *server-side-reactive-web + no-frontend-framework + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* octuple primitive explicitly named in the description + 20 topics.
- A written record of the **server-side-reactive-web primitive**: the *server-driven reactivity* pattern — when a web app is *server-side reactive*, the *server* renders the DOM + *streams DOM-diffs over WebSocket* + *no separate React/Vue/Svelte build step is required*; the *state lives on the server, the client receives the rendered DOM-diffs*; the *server-side-reactive-web* discipline IS the *stateful-connection + server-pushed-DOM-diffs* pattern applied to charter §3.1's *explicit-state-transition* discipline.
- A written record of the **no-SPA + no-JS-build primitive**: the *no-build-step* discipline is the *pure-server-rendering + minimal-client-JS* posture that LE31 v1's HTMX stack already implements (HTMX = pure-HTML + tiny-JS-defer-library; the *no-JS-build* posture is the *no-bundler + no-transpiler + no-package-manager* discipline that LE31 v1 inherits from HTMX).
- A written record of the **WebSocket-stateful-connection primitive**: the *stateful-connection* pattern — when a web app opens a persistent WebSocket connection to the server, the server can *push DOM-diffs* to the client at any time; the *WebSocket-stateful-connection* primitive IS the *server-pushed-state-events* pattern (the *state-changed* event is pushed from server to client over the WebSocket).
- A written record of the **HTTP/2-multiplexed-WebSocket primitive**: the *HTTP/2-multiplexing* discipline — HTTP/2 lets a single TCP connection carry many bidirectional streams; the *HTTP/2-multiplexed-websocket* primitive IS the *stateful-connection-performance* enhancement over LE31 v1's manual-refresh + form-submission posture.
- A written record of the **reverse-proxy-as-application-server primitive**: the *reverse-proxy-server* pattern — duckframework/duck includes a built-in reverse-proxy; LE31 v1's FastAPI + uvicorn does not include a reverse-proxy; the *reverse-proxy-as-application-server* primitive is a future v2 architectural option for any v2 surface that needs to terminate WebSocket connections directly.
- A written record of the **BSD-3-Clause license as a documented strictly-adoptable artifact**: the codebase CAN be adopted as a reference for the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy* pattern (BSD-3-Clause = §3.2 STRICTLY-COMPATIBLE for vocabulary AND architecture-reference adoption); LE31 would re-implement the *server-side-reactive-web* pattern against its own FastAPI + SQLModel + aiogram v3 + Postgres stack, not import the duckframework/duck codebase (LE31 v1 has no server-side-reactive-web surface today).
- A decision record: today's verdict is `defer (parking-lot)` because the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* vocabulary is a v2 no-build reactive-web reference, not a v1 or v2 build implication. v1 has no server-side-reactive-web surface today; the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* vocabulary documents the *future-extension* for the next v2 maintainer asking the *no-build + no-SPA + reactive-UI-over-WebSocket* question.

**Out of scope (defer artifact):**
- Any change to the LE31 v1 surface (no server-side-reactive-web surface today, no WebSocket-stateful-connection today, no HTTP/2-multiplexed-websocket today, no reverse-proxy-as-application-server today).
- Any change to the LE31 v1 waiter web UI (no server-pushed-DOM-diffs today; v1 uses manual-refresh + form-submission).
- Any change to the LE31 v1 cook Telegram bot (no server-pushed-DOM-diffs today; the cook-bot is purely transactional).
- Adoption of the `duckframework/duck` code as a v2 dependency (the repo is 46★/3⑂ at 20529 KB; the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* vocabulary would need to be independently re-implemented and validated against the LE31 v1's no-server-side-reactive-web posture, not simply imported; the duckframework/duck codebase is off-stack for LE31 v1).
- Any change to the `audit_logs` table or `StockEntry` ledger today.
- Any new dependency on the `duckframework` maintainer.

## Evidence / JTBD

When a future LE31 v2 maintainer asks *"if v2 introduces a server-side-reactive-web surface, a WebSocket-stateful-connection primitive, an HTTP/2-multiplexed-websocket primitive, or a reverse-proxy-as-application-server primitive, what is the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* vocabulary that preserves the existing v1 no-server-side-reactive-web + manual-refresh + form-submission posture?"*, the maintainer wants *evidence that another independent 2026 Python repo at 20529 KB with 46★/3⑂ + BSD-3-Clause permissive + in-window-by-push-only + 20 topics including reactive + reactive-ui + real-time + server-side + websocket + http2 + reverse-proxy is shipping the server-side-reactive-web + no-SPA + no-JS-build primitive as the v2 no-build reactive-web discipline*, but struggles because *v1 has no documented server-side-reactive-web primitive in the charter*, so that *v2 can introduce the server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy vocabulary with the explicit charter §3.1 explicit-state-transition discipline rather than reinventing the pattern*.

- **Evidence class**: observed (the `duckframework/duck` description explicitly names *"Build high-performance, scalable, server-side reactive web applications — without a separate frontend framework or a complex JavaScript stack"* = the server-side-reactive-web + no-SPA + no-JS-build primitive; the 20 topics include reactive + reactive-ui + real-time + server-side + websocket + http2 + reverse-proxy = 7 LE31-relevant topics; the BSD-3-Clause license + 46★/3⑂ + 20529 KB substantial + in-window-by-push-only = §3.2 STRICTLY-COMPATIBLE).
- **Confidence**: medium for the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* vocabulary (BSD-3-Clause + Python + 20529 KB substantial + in-window-by-push-only + 46★/3⑂ + 20 topics + explicit server-side-reactive-web + no-SPA + no-JS-build description + 7 LE31-relevant topics = §3.2 STRICTLY-COMPATIBLE + §3.4 not triggered; the *server-side-reactive-web* discipline IS the *stateful-connection + server-pushed-DOM-diffs* pattern applied to charter §3.1's *explicit-state-transition* discipline).
- **Real observed LE31 JTBD**: none directly. The cross-section is *primitive vocabulary extension + server-side-reactive-web + no-SPA + no-JS-build documentation*, not *LE31 demand*.
- **The value is primitive vocabulary extension + server-side-reactive-web + no-SPA + no-JS-build documentation**: when (if) LE31 v2 introduces a server-side-reactive-web surface, a WebSocket-stateful-connection primitive, an HTTP/2-multiplexed-websocket primitive, or a reverse-proxy-as-application-server primitive, the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* vocabulary is documented.

## Description

GitHub `duckframework/duck` (BSD-3-Clause, 46★/3⑂, Python, pushed 2026-09-17T17:55:19Z = in-window by `pushed_at` only; created 2022-12-04T11:54:27Z = out-of-window, ~3.8 years before the 2026-08-22 window-start, 20529 KB substantial repo).

Description (verbatim): *"Build high-performance, scalable, server-side reactive web applications — without a separate frontend framework or a complex JavaScript stack."*

Topics (verbatim from raw JSON, 20 topics): `async`, `backend`, `developer-tools`, `full-stack`, `high-performance`, `http2`, `lively-ui`, `modern-web`, `open-source`, `python3`, `reactive`, `reactive-ui`, `real-time`, `reverse-proxy`, `scalable`, `server-side`, `web-development`, `web-framework`, `web-server`, `websocket`.

The **7 LE31-relevant topics**: `reactive + reactive-ui + real-time + server-side + websocket + http2 + reverse-proxy`.

The **8 named no-build reactive-web primitives**:

| duckframework/duck primitive | LE31 v1 primitive | Match status |
|---|---|---|
| `server-side` (server-driven reactivity) | (LE31 v1 has no server-driven reactivity today; v1 uses HTMX which is client-driven-HTML-overrides) | **★ GAP: LE31 v1 has no server-driven reactivity today** |
| `reactive` + `reactive-ui` (reactive-UI-over-WebSocket) | (LE31 v1 has no reactive-UI today; v1 uses manual-refresh + form-submission) | **★ GAP: LE31 v1 has no reactive-UI today** |
| `websocket` (stateful-connection primitive) | (LE31 v1 has no WebSocket-stateful-connection today; v1 uses SSE for cook channel = feature 23) | **★ GAP: LE31 v1 has no WebSocket-stateful-connection today** |
| `real-time` (real-time event-stream) | (LE31 v1 has no real-time-event-stream today; cook-bot uses Telegram long-polling = feature 23) | **★ GAP: LE31 v1 has no real-time-event-stream today** |
| `http2` (HTTP/2-multiplexing primitive) | (LE31 v1 uses HTTP/1.1 today; v1 has no HTTP/2-multiplexing today) | **★ GAP: LE31 v1 has no HTTP/2-multiplexing today** |
| `reverse-proxy` (reverse-proxy-as-application-server primitive) | (LE31 v1's FastAPI + uvicorn does not include a reverse-proxy) | **★ GAP: LE31 v1 has no reverse-proxy-as-application-server today** |
| `lively-ui` (lively-UI-over-server-side-rendering) | (LE31 v1 has no lively-UI today; v1 uses HTMX which is HTML-overrides) | **★ GAP: LE31 v1 has no lively-UI today** |
| `no-frontend-framework` + `no-javascript-stack` (no-SPA + no-JS-build posture) | (LE31 v1 already implements no-SPA + no-JS-build via HTMX) | **★ MATCH: LE31 v1 already implements the no-SPA + no-JS-build posture** |

The **server-side-reactive-web primitive**: the *server-driven reactivity* pattern — when a web app is *server-side reactive*, the *server* renders the DOM + *streams DOM-diffs over WebSocket* + *no separate React/Vue/Svelte build step is required*; the *state lives on the server, the client receives the rendered DOM-diffs*; the *server-side-reactive-web* discipline IS the *stateful-connection + server-pushed-DOM-diffs* pattern applied to charter §3.1's *explicit-state-transition* discipline — the server is the source of truth; the client receives DOM-diffs.

The **no-SPA + no-JS-build primitive**: the *no-build-step* discipline is the *pure-server-rendering + minimal-client-JS* posture that LE31 v1's HTMX stack already implements (HTMX = pure-HTML + tiny-JS-defer-library; the *no-JS-build* posture is the *no-bundler + no-transpiler + no-package-manager* discipline that LE31 v1 inherits from HTMX).

The **WebSocket-stateful-connection primitive**: the *stateful-connection* pattern — when a web app opens a persistent WebSocket connection to the server, the server can *push DOM-diffs* to the client at any time; the *WebSocket-stateful-connection* primitive IS the *server-pushed-state-events* pattern (the *state-changed* event is pushed from server to client over the WebSocket).

The **HTTP/2-multiplexed-WebSocket primitive**: the *HTTP/2-multiplexing* discipline — HTTP/2 lets a single TCP connection carry many bidirectional streams; the *HTTP/2-multiplexed-websocket* primitive IS the *stateful-connection-performance* enhancement over LE31 v1's manual-refresh + form-submission posture.

The **reverse-proxy-as-application-server primitive**: the *reverse-proxy-server* pattern — duckframework/duck includes a built-in reverse-proxy; LE31 v1's FastAPI + uvicorn does not include a reverse-proxy; the *reverse-proxy-as-application-server* primitive is a future v2 architectural option for any v2 surface that needs to terminate WebSocket connections directly.

The **BSD-3-Clause license as a documented strictly-adoptable artifact**: the codebase CAN be adopted as a reference for the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy* pattern (BSD-3-Clause = §3.2 STRICTLY-COMPATIBLE for vocabulary AND architecture-reference adoption); LE31 would re-implement the *server-side-reactive-web* pattern against its own FastAPI + SQLModel + aiogram v3 + Postgres stack, not import the duckframework/duck codebase (LE31 v1 has no server-side-reactive-web surface today).

## Data model

**No LE31 data model change today.** The defer artifact is documentation only. The *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* primitive names the *server-driven-reactivity + WebSocket-stateful-connection + HTTP/2-multiplexing + reverse-proxy-server* extensions that any future v2 schema change should consider; v2 maintainer decision required before adopting the server-side-reactive-web surface (LE31 v1 currently operates no server-side-reactive-web surface; server-side-reactive-web is a v2 architecture-decision, not a v1 implementation task).

## Implementation steps

**NONE today.** The defer artifact is documentation only. Files to touch *if* the future v2 trigger condition fires (first v2 PR that adds a server-side-reactive-web surface to the LE31 v2 operator surface, a WebSocket-stateful-connection primitive to the LE31 v2 architecture, an HTTP/2-multiplexed-websocket primitive, or a reverse-proxy-as-application-server primitive):

- `app/web/reactive/` — possibly add (the server-side-reactive-web module; depends on the v2 change).
- `app/web/reactive/websocket.py` — possibly add (the WebSocket-stateful-connection primitive that pushes DOM-diffs to clients; depends on the v2 change).
- `app/web/reactive/http2.py` — possibly add (the HTTP/2-multiplexing primitive; depends on the v2 change).
- `app/web/reverse_proxy.py` — possibly add (the reverse-proxy-as-application-server primitive; depends on the v2 change).
- `app/web/reactive/state.py` — possibly add (the server-side-state primitive that tracks the source-of-truth state on the server; depends on the v2 change).
- `tests/test_reactive_websocket.py` + `tests/test_http2_multiplexing.py` + `tests/test_reverse_proxy.py` + `tests/test_reactive_state.py` — possibly add (the integration tests for the server-side-reactive-web + WebSocket-stateful-connection + HTTP/2-multiplexed-websocket + reverse-proxy-as-application-server primitives; depends on the v2 change).

**No existing file changes today.** No migration, no SQLModel schema change, no FastAPI route change, no Telegram handler change, no server-side-reactive-web surface added.

## Telegram interaction if any

None today. If a future v2 trigger fires (server-side-reactive-web surface), the Telegram interaction could be extended with reactive-web-notifications: the cook sees a server-pushed-DOM-diff when the order status changes; the cook marks the order `ready` via the cook-bot; the server pushes a DOM-diff to the waiter's browser over the WebSocket; the waiter picks up the order from the expo-pass. The *server-pushed-DOM-diffs* discipline ensures the waiter sees the order status in real-time without manual-refresh; the *WebSocket-stateful-connection* discipline ensures the connection is persistent; the *HTTP/2-multiplexing* discipline ensures the connection is performant.

## Dependencies

- No new dependency today.
- If a future v2 trigger fires:
  - Software: a WebSocket library (e.g. FastAPI's built-in WebSocket support; LE31 currently has no WebSocket dependency).
  - Software: an HTTP/2 library (e.g. `httpx` for HTTP/2 client; LE31 currently uses `httpx` 0.28.1).
  - Software: a reverse-proxy library (e.g. `nginx` or `traefik`. Lease the LE31 v1 charter §3.2 stack-invariant = Postgres in prod + Python 3.13 + FastAPI + SQLModel + aiogram v3).
  - Schema: a `web_socket_session` table for WebSocket-session tracking + a `web_socket_state` table for server-side-state; depends on the v2 change.

## Open questions

- Should LE31 v2 introduce a server-side-reactive-web surface (e.g. `app/web/reactive/`)?
- Should LE31 v2 introduce a WebSocket-stateful-connection primitive (e.g. `app/web/reactive/websocket.py`)?
- Should LE31 v2 introduce an HTTP/2-multiplexed-websocket primitive (e.g. `app/web/reactive/http2.py`)?
- Should LE31 v2 introduce a reverse-proxy-as-application-server primitive (e.g. `app/web/reverse_proxy.py`)?
- If yes to any of the above: should the v2 PR cross-reference this feature 210 contract + HANDOFF as the named architectural reference?

## Why this matters

The *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* primitive is the **gap** between LE31 v1's no-server-side-reactive-web + manual-refresh + form-submission posture and a *server-side-reactive-web + WebSocket-stateful-connection + HTTP/2-multiplexing + reverse-proxy-server* v2 surface. The *server-side-reactive-web* discipline IS the *charter §3.1 explicit-state-transition discipline* applied to the server-driven-reactivity dimension; the *WebSocket-stateful-connection* discipline IS the *charter §3.1 explicit-state-transition discipline* applied to the stateful-connection dimension. For LE31 v2's server-side-reactive-web + WebSocket-stateful-connection + HTTP/2-multiplexing + reverse-proxy-server surface, the duckframework/duck pattern is the *future-extension reference*: when (if) LE31 v2 introduces a server-side-reactive-web surface, a WebSocket-stateful-connection primitive, an HTTP/2-multiplexed-websocket primitive, or a reverse-proxy-as-application-server primitive, the *server-side-reactive-web + no-SPA + no-JS-build + WebSocket + HTTP/2 + reverse-proxy + reactive-ui + real-time* vocabulary is documented. **The largest in-window 2026 Python server-side-reactive-web repo of the 53-pass series** is the *strongest no-build reactive-web data point* of the 53-pass series; the BSD-3-Clause license is §3.2 STRICTLY-COMPATIBLE for vocabulary + architecture-reference adoption.