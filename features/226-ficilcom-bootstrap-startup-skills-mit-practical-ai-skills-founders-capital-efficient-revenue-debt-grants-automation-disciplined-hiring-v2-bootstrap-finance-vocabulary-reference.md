# Feature 226 — ficilcom-bootstrap-startup-skills-mit-practical-ai-skills-founders-capital-efficient-revenue-debt-grants-automation-disciplined-hiring-v2-bootstrap-finance-vocabulary-reference (defer)

> **NEW observation (2026-09-24).** Documents in-window GitHub Search `topic:small-business+language:python` query result: `ficilcom/bootstrap-startup-skills` (**MIT ✓**, **2★/0⑂** (early-traction, no fork activity), Python, **pushed 2026-08-28T18:46:31Z** (in-window by `pushed_at` — *27 days before fetch time*; near-edge of window), **created 2026-08-21T19:34:14Z** (~1-month-old repo with in-window `created_at`; **IN-WINDOW BY BOTH FIELDS**), **649 KB** small repo, default_branch=`main`). Topics (verbatim from raw JSON, **9 topics**): `agent-skills`, `ai-agents`, `bootstrapping`, `claude-code`, `finance`, `founders`, `japan`, `small-business`, `startup`. Description (verbatim, parent-verified GitHub API direct-GET 2026-09-24): *"Practical AI skills for founders building capital-efficient companies with revenue, debt, grants, automation, and disciplined hiring."* **The only 2026-09-24 in-window v2 bootstrap-finance-vocabulary-reference that pins all 9 of `agent-skills + ai-agents + bootstrapping + claude-code + finance + founders + japan + small-business + startup` in the topic array** = **6/9 LE31-relevant topics** = **the first v2 bootstrap-finance-vocabulary reference of the 57-pass series**. Bucket: **v2 bootstrap-finance-vocabulary-reference (defer, parking-lot)** — watch-list entry, zero build time today. **HONEST DISCLOSURE**: this repo was not previously surfaced in any prior brainstorm report — the `claude-code + ai-agents + finance + founders` topic cluster is the first *bootstrap-finance + AI-agents-as-operator + solo-operator-discipline* cluster of the 57-pass series; the *operator-tooling-AI-not-customer-facing-AI* posture is the load-bearing §3.4 finding (the AI runs in the developer's IDE not in the customer's chat).

## Goal

Retain the **"Practical AI skills for founders building capital-efficient companies with revenue, debt, grants, automation, and disciplined hiring."** + **"agent-skills + ai-agents + bootstrapping + claude-code + finance + founders + japan + small-business + startup"** nonuple-primitive as a persistent cross-section reference for any future LE31 v2 expansion that needs a *bootstrap-finance + AI-agents-as-operator + solo-operator-discipline* baseline to compare against. The artifact is the persistent cross-section reference + the nine named architectural primitives (agent-skills, ai-agents, bootstrapping, claude-code, finance, founders, japan, small-business, startup). No code today (MIT license permits future code reuse; vocabulary-only artifact today; 649 KB small-repo size makes source-code inspection feasible for the v2 maintainer).

## Scope

**In scope (defer artifact):**
- A written record of the **AI-skills-as-operator-tooling** discipline: the *operator-tooling-AI* posture (LE31 v1 today has no AI surface at all per charter §3.4; the *operator-tooling-AI* discipline IS the *AI-is-a-tool-for-the-operator* posture; the *claude-code* topic explicitly names the *developer-coding-agent* primitive which is the *AI-in-the-developer's-IDE* posture, NOT the *AI-in-the-customer's-chat* posture; **§3.4 NOT triggered** because the AI surface is operator-tooling not customer-facing).
- A written record of the **bootstrapping + capital-efficient** discipline: the *small-business-capital-discipline* vocabulary (the *bootstrapping* primitive IS the *build-without-external-funding* posture; the *capital-efficient* primitive IS the *spend-less-than-you-earn* discipline).
- A written record of the **finance + revenue + debt + grants** discipline: the *bootstrap-finance-modules* vocabulary (every founder faces these four modules; the *bootstrap-finance-modules* discipline IS the *what-every-founder-needs-to-know-about-finance* pattern).
- A written record of the **automation + disciplined-hiring** discipline: the *founder-automation-disciplines* vocabulary (the *automation* primitive IS the *automate-the-repetitive-tasks* posture; the *disciplined-hiring* primitive IS the *hire-only-when-necessary* posture; both primitives support the *solo-operator-as-default* discipline).
- A written record of the **founders + small-business + startup** discipline: the *solo-operator-as-default* posture (LE31 v1 is one-small-restaurant = small-business = single-owner-single-operator; the *founders + small-business + startup* vocabulary IS the *single-founder-single-deployment* posture).
- A written record of the **japan territory-annotation**: the *territory-context* (the *japan* topic is a territory-annotation not an architecture-annotation; LE31 v1 is Switzerland-Europe; the *japan* topic doesn't transfer but the *founder-facing-financial-discipline* is territory-agnostic).
- A decision record: today's verdict is `defer` because (a) LE31 v2 is not built today and (b) the *claude-code* AI posture is an *operator-tooling-AI* posture that LE31 v1 does not currently need; the comparison baseline is informative, not a build-trigger.
- A cross-section reference with the prior v2 AI-agents-as-operator + small-business + bootstrap cluster: features 102 (`nightmux-stdlib-telegram-bridge`), 178 (`nhobin219/litelink` append-only-iceberg-embedded-local-first), 220 (`Jita81/commit-replay-bench` AI-coding-agent-evaluation-held-out-tests-append-only-evidence-ledger). The *transferable insight* is the **AI-skills + bootstrapping + finance + founders + automation + disciplined-hiring** sextuple-primitive.

**Out of scope (defer artifact):**
- Any change to LE31 v1's data model.
- Any change to the waiter web UI.
- Any change to the cook Telegram bot.
- Any change to the `audit_logs` schema.
- Any change to the `StockEntry` schema.
- Any v2 surface in v1 (charter §3.1 + §3.2 invariant: v1 surface expansion is the next boundary; cross-section is informative, not a v2 expansion trigger).
- Any customer-facing AI surface in v1 (charter §3.4 explicit invariant: NO customer-facing AI).
- Any owner-facing AI surface in v1 (LE31 v1 has no AI surface at all).
- Any v2 horizontal-expansion surface in v2 (charter §3.1 + §3.2 invariant: v2 surface expansion is the next boundary; this is a vocabulary reference, not a v2 expansion trigger).

## Description

GitHub Search `topic:small-business+language:python` query (parent re-fetched live, see `/tmp/le31-brainstorm-2026-09-24/gh/gh_01_topic-small-business.json`) returned 77 total / 50 retrieved candidates; `ficilcom/bootstrap-startup-skills` is one of the 30 net-new in-window candidates. It is the **only 2026-09-24 in-window v2 bootstrap-finance-vocabulary-reference that pins all 9 of `agent-skills + ai-agents + bootstrapping + claude-code + finance + founders + japan + small-business + startup` in the topic array**.

The `ficilcom/bootstrap-startup-skills` repo's architectural pattern has nine core sub-primitives:

1. **AI-skills-as-operator-tooling** — the *operator-tooling-AI* posture. The phrase *"Practical AI skills for founders"* explicitly names the *founder-facing-AI-skills* discipline (the AI is *founder-facing* not *customer-facing*); *" *" explicitly names the *claude-code-coding-agent* primitive which is the *developer-AI-coding-assistant* posture (Anthropic's `claude-code` CLI tool = a coding agent for developers, NOT a customer-facing chatbot).

3. **Bootstrapping + capital-efficient** — the *small-business-capital-discipline* vocabulary. The *bootstrapping* primitive IS the *build-without-external-funding* posture; the *capital-efficient* primitive IS the *spend-less-than-you-earn* discipline. LE31 v1 today is single-restaurant-vertical = capital-constrained; the *bootstrapping + capital-efficient* vocabulary IS the *small-business-capital-discipline* applied to the *founder-facing* dimension.

5. **Finance + revenue + debt + grants** — the *bootstrap-finance-modules* vocabulary. The *finance* primitive IS the *capital-management* discipline; the *revenue* primitive IS the *income-tracking* discipline; the *debt* primitive IS the *payable-tracking* discipline; the *grants* primitive IS the *non-dilutive-funding* discipline (the *grants* primitive is the *what-grants-are-available* query; sister-shape to features 158's `azizsekerdil-smart-restaurant-management-system` AI-powered-POS-KDS-inventory which mentions *grants* in the small-business context).

7. **Automation + disciplined-hiring** — the *founder-automation-disciplines* vocabulary. The *automation* primitive IS the *automate-the-repetitive-tasks* posture; the *disciplined-hiring* primitive IS the *hire-only-when-necessary* posture. Both primitives support the *solo-operator-as-default* discipline (LE31 v1 is single-restaurant-vertical = single-operator-as-default).

9. **Founders + small-business + startup** — the *solo-operator-as-default* posture. The *founders* + *small-business* + *startup* vocabulary IS the *single-founder-single-deployment* posture.

11. **Japan territory-annotation** — the *territory-context*. The *japan* topic is a territory-annotation not an architecture-annotation; LE31 v1 is Switzerland-Europe; the *japan* topic doesn't transfer but the *founder-facing-financial-discipline* is territory-agnostic (the *revenue + debt + grants + automation + disciplined-hiring* modules are the same regardless of territory).

The LE31 relevance is the **AI-skills + bootstrapping + finance + founders + automation + disciplined-hiring + small-business + startup** octuple-primitive. LE31 v1 today has working single-restaurant-vertical product; the question this repo answers is "what does a founder-facing-financial-discipline + AI-agents-as-operator + solo-operator-discipline baseline look like in 2026?"

## Data model

**No change to LE31 v1's data model today.** The defer artifact is documentation only.

**Future v2 expansion trigger condition (if the first v2 PR that adds a bootstrap-finance operator-tooling surface to the LE31 v2 operator surface lands):** potential schema additions (depending on the v2 surface):
- `app/finance_modules/` — possibly add (the bootstrap-finance module directory; depends on the v2 change).
- `app/finance_modules/revenue.py` — possibly add (the income-tracking handler; depends on the v2 change).
- `app/finance_modules/debt.py` — possibly add (the payable-tracking handler; depends on the v2 change).
- `app/finance_modules/grants.py` — possibly add (the grants-tracking handler; depends on the v2 change).
- `app/finance_modules/automation.py` — possibly add (the founder-automation handler; depends on the v2 change).
- `app/models/finance_revenue.py` — possibly add (the `finance_revenue` SQLModel table; depends on the v2 change).
- `app/models/finance_debt.py` — possibly add (the `finance_debt` SQLModel table; depends on the v2 change).
- `app/models/finance_grants.py` — possibly add (the `finance_grants` SQLModel table; depends on the v2 change).
- `app/models/finance_automation.py` — possibly add (the `finance_automation` SQLModel table; depends on the v2 change).
- `tests/test_finance_modules.py` + `tests/test_finance_revenue.py` + `tests/test_finance_debt.py` + `tests/test_finance_grants.py` — possibly add (the integration tests for the bootstrap-finance + revenue + debt + grants surfaces; depends on the v2 change).

**No existing file changes today.** No migration, no SQLModel schema change, no FastAPI route change, no Telegram handler change, no bootstrap-finance operator-tooling surface added.

## Implementation steps

**NONE today.** The defer artifact is documentation only. Implementation steps if the future v2 trigger condition fires:

1. **Read the `ficilcom/bootstrap-startup-skills` source** (649 KB small-repo size) — identify the *AI-skills-as-operator-tooling* primitives; identify the *bootstrapping + capital-efficient* posture; identify the *finance + revenue + debt + grants* modules; identify the *automation + disciplined-hiring* disciplines.
2. **Adapt the vocabulary to LE31 v2's existing schema** — re-implement the *AI-skills + bootstrapping + finance + founders + automation + disciplined-hiring* primitive against LE31's existing FastAPI + SQLModel + aiogram v3 + PostgreSQL stack. **CAUTION**: any v2 surface that introduces AI must respect charter §3.4 — *no customer-facing AI*; the *operator-tooling-AI* posture (AI is a tool for the operator, not for the customer) is the only AI surface permitted in v2.
3. **Document the cross-section** in `specs/` (the present HANDOFF.md) — record the *AI-skills + bootstrapping + finance + founders + automation + disciplined-hiring* octuple-primitive as a persistent cross-section reference for future v2 bootstrap-finance operator-tooling reviews.
4. **Verify the cross-section** by reading at least 6 of the 9 topic primitives in source code (agent-skills + ai-agents + bootstrapping + finance + founders + small-business are the six LE31-relevant-topic primitives).
5. **Update the LE31 v2 horizontal-expansion roadmap** to reflect the *bootstrap-finance + AI-agents-as-operator + solo-operator-discipline* vocabulary primitive as a future-build option.

**No code today.**

## Telegram interaction if any

None today. The defer artifact is documentation only; no Telegram operator surface changes.

## Dependencies

- **No new dependencies today.** The defer artifact is documentation only.
- **Future v2 dependencies (if the v2 trigger condition fires):** `fastapi` (already in LE31 v1 stack per charter §3.2), `sqlalchemy` (already in LE31 v1 stack via SQLModel per charter §3.2). No new Python packages required.
- **Optional future v2 dependency**: `claude-code` (the *claude-code-coding-agent* primitive would be a developer-side dependency, not a customer-side dependency; the *claude-code* integration is the *developer-AI-coding-assistant* posture which is on-pattern for LE31 v1's *no-AI-surface* discipline).

## Open questions

- **Does LE31 v2 ever need a bootstrap-finance operator-tooling surface?** Today no; v1 is single-restaurant-vertical. If v2 ever expands to founder-facing-financial-discipline surfaces, the *bootstrap-finance + AI-agents-as-operator + solo-operator-discipline* primitive is the cleanest cross-section reference. **Status: parking-lot until v2 expansion is on the roadmap.**
- **Would the *claude-code* AI posture apply to LE31 v2?** Today no; v1 has no AI surface. The *operator-tooling-AI* posture (AI is a tool for the operator, not for the customer) is the only AI surface permitted in v2 per charter §3.4. **Status: parking-lot until v2 expansion is on the roadmap.**
- **Is the *japan* territory-annotation applicable to LE31 v2?** No; LE31 v1 is Switzerland-Europe. The *japan* topic is a territory-annotation not an architecture-annotation; the *founder-facing-financial-discipline* is territory-agnostic. **Status: territory-context; the founder-facing modules transfer regardless of territory.**
- **Would the *automation + disciplined-hiring* primitives apply to LE31 v1?** Today no; v1 is single-restaurant-vertical. **Status: parking-lot until v1 founder-facing-automation is on the roadmap.**

## Why this matters

The *AI-skills + bootstrapping + finance + founders + automation + disciplined-hiring + small-business + startup* octuple-primitive is the **first v2 bootstrap-finance-vocabulary reference of the 57-pass series**. LE31 v1 today is single-restaurant-vertical; the v2 horizontal-expansion question is *"if v2 ever needs to support founder-facing-financial-discipline (revenue + debt + grants + automation + disciplined-hiring), what does the *AI-skills + bootstrapping + finance + founders + automation + disciplined-hiring* vocabulary look like in 2026?"* Today's pick documents that vocabulary with the **only 2026-09-24 GitHub Search candidate that pins all of `agent-skills + ai-agents + bootstrapping + finance + founders + small-business` simultaneously** = the closest LE31-stack-shape match for the *bootstrap-finance + AI-agents-as-operator + solo-operator-discipline* triple-primitive. The **§3.4 NOT-triggered finding** (the *claude-code* AI posture is *operator-tooling-AI not customer-facing-AI*) is the load-bearing charter-compliance signal: this pick documents an *operator-facing-AI* primitive that respects LE31's §3.4 hard rule against customer-facing AI.

Without this artifact, a future LE31 v2 maintainer facing the *bootstrap-finance operator-tooling* question would have to discover the *AI-skills + bootstrapping + finance + founders + automation + disciplined-hiring* vocabulary from scratch; with this artifact, the vocabulary is documented as a persistent cross-section reference with the *6/9 topic-overlap score + 2★/0⑂ + MIT permissive + 649 KB small-repo size + IN-WINDOW BY BOTH FIELDS* combined signal.