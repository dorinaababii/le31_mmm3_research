# Feature 235 — `maojiebc-majia-huiyuan-mit-bi-data-warehouse-membership-restaurant-retail-agent-skills-llm-friendly-chinese-v2-ai-owner-analytics-vocabulary-reference` (defer)

> **NEW observation (2026-09-26).** Documents in-window GitHub Search `topic:small-business+language:python` query result: `maojiebc/majia-huiyuan` (**MIT ✓**, **12★/2⑂** (the most-starred of today's 3 picks), Python, **pushed 2026-09-14T03:55:14Z** (in-window by `pushed_at` — *12 days before fetch time*; mid-window), **created 2026-07-11T17:13:08Z** (~2.5-month-old repo with in-window `pushed_at`; **IN-WINDOW BY PUSH ONLY**; `created_at` is OUT-OF-WINDOW by ~6 weeks), **11001 KB** substantial repo, default_branch=`main`). Topics (verbatim from raw JSON, **12 topics**): `agent-skills`, `bi`, `chinese`, `crm`, `data-warehouse`, `guandata`, `llm-friendly`, `membership`, `open-data`, `restaurant`, `retail`, `workbuddy`. Description (verbatim, parent-verified GitHub API direct-GET 2026-09-26): *"会员运营 · 马甲实战版 — 会员指标、券批次、激励费用与储值分析参考 · v1.4.5"* = *"membership-operations · frontline-operator-practical-edition — member-metrics, coupon-batches, incentive-cost, stored-value-analytics reference · v1.4.5"*. **The only 2026-09-26 in-window v2-AI owner-analytics-vocabulary-reference that pins all 8 of `agent-skills + bi + data-warehouse + llm-friendly + membership + open-data + restaurant + retail` in the topic array** = **8/12 LE31-relevant topics** = **the first v2-AI owner-analytics-vocabulary of the 58-pass series**. Bucket: **v2-AI owner-analytics-vocabulary-reference (defer, parking-lot)** — watch-list entry, zero build time today. **HONEST DISCLOSURE**: the *chinese + crm + guandata + workbuddy* surface is **off-pattern for LE31 v1's European-restaurant posture** per charter §3.1 (single-restaurant, France, EUR, Paris timezone); the pick is filed as a **vocabulary reference for the BI + data-warehouse + membership + agent-skills + llm-friendly primitives**, NOT as a v1 or v2 surface that LE31 would adopt wholesale; the *agent-skills + llm-friendly* primitive cluster is the *operator-tooling-AI* posture — §3.4 NOT triggered because the AI surface is *owner-analytics* not customer-facing.

## Goal

Retain the **"会员运营 · 马甲实战版 — 会员指标、券批次、激励费用与储值分析参考 · v1.4.5"** + **"agent-skills + bi + chinese + crm + data-warehouse + guandata + llm-friendly + membership + open-data + restaurant + retail + workbuddy"** duodecuple-primitive as a persistent v2-AI owner-analytics-vocabulary-reference for any future LE31 v2 maintainer asking the *BI + data-warehouse + membership + LLM-friendly-analytics + owner-tooling-AI-not-customer-facing-AI* question (does LE31 v2 introduce a daily-P&L dashboard derived from `StockEntry`? does LE31 v2 introduce a membership-loyalty surface? does LE31 v2 introduce a weekly-business-review workflow? does LE31 v2 expose `audit_logs` + `StockEntry` as LLM-friendly JSON for owner AI-assist queries?). The artifact is the persistent *BI + data-warehouse + membership + agent-skills + llm-friendly + open-data + owner-tooling-AI* vocabulary as a *named* v2-AI owner-analytics reference. No code today (MIT license permits future code reuse; vocabulary-only artifact today; 11001 KB substantial-repo size makes source-code inspection feasible for the v2 maintainer).

## Scope

**In scope (defer artifact):**
- A written record of the **BI + data-warehouse + open-data** discipline: the *business-intelligence + data-warehouse + open-data* primitive (LE31 v1 has no BI surface today; the *data-warehouse-as-derived-tables-from-StockEntry* primitive IS a v2 architectural option; charter §3.1's *append-only-StockEntry-ledger* discipline maps directly onto a *data-warehouse-as-derived-tables-from-StockEntry* primitive where the `StockEntry` table is the *source-of-truth* and the *data-warehouse* tables are the *derived-views*; the *open-data* topic explicitly names the *open-data-posture* — the *data-warehouse-tables-are-open-to-the-owner-AI-assist* discipline).
- A written record of the **membership + restaurant + retail** discipline: the *membership-loyalty + restaurant + retail* primitive (LE31 v1 has no membership-loyalty surface today; the *membership-as-LE31-v2-horizontal-expansion-surface* discipline IS a v2 architectural option; the *restaurant + retail* pair is the *restaurant-and-retail-cross-vertical* vocabulary that LE31 v2 may or may not adopt; today LE31 v1 is single-restaurant-vertical per charter §3.1).
- A written record of the **agent-skills + llm-friendly** discipline: the *AI-skill-as-package + LLM-friendly-data-shape* primitive (the *agent-skills + llm-friendly* combination IS the *AI-skill-as-package + LLM-friendly-data-shape* pattern that maps onto LE31 v2's charter §3.4 *AI-may-assist-owner-with-observable-evidence* posture; each *LLM-friendly* JSON export endpoint IS a *discrete AI-skill* that the owner-facing-AI-tool can invoke; the *non-AI-fallback* is the underlying BI/dashboard that works without the AI layer).
- A written record of the **会员指标 (member-metrics) + 券批次 (coupon-batches) + 激励费用 (incentive-cost) + 储值分析 (stored-value-analytics)** discipline: the *membership-analytics* primitive (the *member-metrics + coupon-batches + incentive-cost + stored-value-analytics* quartet IS the *membership-loyalty-analytics* vocabulary that LE31 v2 would need if (or when) it introduces a membership-loyalty surface; charter §3.1's *append-only-StockEntry-ledger* discipline maps onto a *stored-value-as-append-only-StockEntry* primitive where each *stored-value-credit* and *stored-value-debit* is a new `StockEntry`).
- A cross-section reference with the prior BI + data-warehouse + membership + small-business + agent-skills cluster: features 158 (`azizsekerdil-smart-restaurant-management-system`) + 190 (`ilyautov/small-business-ru` operator-AI owner-assist) + 195 (`skaslam1407-Restaurant-and-Cloud-Kitchen-Operations-Management`) + 203 (`shawn-durrani/membro` v2-AI control-plane) + 224 (`celerp/celerp` self-hosted-FastAPI-ERP-vocabulary) + 225 (`anugrhaswi-shop-ledger` small-business-bookkeeping-vocabulary) + 226 (`ficilcom/bootstrap-startup-skills` v2 bootstrap-finance-vocabulary). The *transferable insight* is the **BI + data-warehouse + membership + restaurant + retail + agent-skills + llm-friendly + open-data + owner-tooling-AI-not-customer-facing-AI** nonuple-primitive.
- A decision record: today's verdict is `defer (parking-lot)` because (a) LE31 v1 has no BI surface today (charter §3.1 *single-restaurant-vertical* discipline + charter §3.2 *append-only-StockEntry-ledger* discipline + charter §3.4 *no-customer-facing-AI* discipline); (b) the *chinese + crm + guandata + workbuddy* surface is off-pattern for v1's European-restaurant posture; (c) the *BI + data-warehouse + membership + agent-skills + llm-friendly* primitives are *forward-looking* primitives that LE31 v2 may adopt when (or if) it introduces an *owner-analytics* surface.

**Out of scope (defer artifact):**
- Any change to LE31 v1's data model.
- Any change to the waiter web UI.
- Any change to the cook Telegram bot.
- Any change to the `audit_logs` schema.
- Any change to the `StockEntry` schema.
- Any v2 surface in v1 (charter §3.1 + §3.2 invariant: v1 surface expansion is the next boundary; cross-section is informative, not a v2 expansion trigger).
- Any customer-facing AI surface in v1 (charter §3.4 explicit invariant: NO customer-facing AI).
- Any owner-facing AI surface in v1 (LE31 v1 has no AI surface at all).
- Adoption of majia-huiyuan's source code as a v2 dependency (majia-huiyuan's *chinese + crm + guandata + workbuddy* surface is Chinese-market-CRM-specific; the *BI + data-warehouse + membership + agent-skills + llm-friendly* primitives would need to be independently re-implemented and validated against LE31's existing FastAPI + SQLModel + Postgres stack, not simply imported). The cross-section is *primitive vocabulary extension + architecture-reference*, not *library adoption*.
- Any third-party-data-source dependency (charter §3.1 *single-restaurant-vertical* discipline: LE31 v1 has no Guandata-equivalent dependency today).
- Any membership-loyalty surface in v1 (LE31 v1 is *single-restaurant-vertical* per charter §3.1; membership-loyalty is a *horizontal-expansion* surface that v2 may adopt).

## Description

`maojiebc/majia-huiyuan` is a 11001 KB Python repository that implements a *membership-operations frontline-operator-practical-edition* BI/data-warehouse platform with four explicit modules: **member-metrics** (会员指标), **coupon-batches** (券批次), **incentive-cost** (激励费用), and **stored-value-analytics** (储值分析). The platform's design philosophy is *owner-analytics as BI + data-warehouse + LLM-friendly*: the BI/dashboard surface is *owner-facing* (the *owner-analytics* posture — §3.4 NOT triggered because the AI surface is owner-analytics not customer-facing). The 12-topic array includes `agent-skills + bi + chinese + crm + data-warehouse + guandata + llm-friendly + membership + open-data + restaurant + retail + workbuddy`; the *bi + data-warehouse* pair is the *business-intelligence + data-warehouse* primitive; the *membership + restaurant + retail* triplet is the *membership-loyalty + restaurant + retail* vocabulary; the *agent-skills + llm-friendly* pair is the *AI-skill-as-package + LLM-friendly-data-shape* primitive; the *open-data* topic explicitly names the *open-data-posture* — the *data-warehouse-tables-are-open-to-the-owner-AI-assist* discipline. The majia-huiyuan pattern is the *future-v2-AI-architecture-vocabulary* for LE31 v2's owner-analytics-evolution.

## Data model

N/A (defer artifact; vocabulary-only; no schema change). If a future v2 PR adopts the majia-huiyuan *BI + data-warehouse + membership + agent-skills + llm-friendly* primitive for owner-analytics, the data-model impact would be a *data-warehouse-as-derived-tables-from-StockEntry* schema with these new derived tables:
- `daily_pnl` (date, revenue_eur, cost_eur, profit_eur, computed_at)
- `membership_metrics` (member_id, visit_count, total_spend_eur, last_visit_at, computed_at)
- `coupon_batch` (batch_id, coupon_type, issued_count, redeemed_count, incentive_cost_eur, computed_at)
- `stored_value_analytics` (member_id, balance_eur, credit_count, debit_count, computed_at)
The *derived-tables-from-StockEntry* discipline preserves charter §3.1's *append-only-StockEntry-ledger* invariant (the `StockEntry` table remains the *source-of-truth*; the *data-warehouse* tables are the *derived-views* that can be regenerated from `StockEntry` at any time).

## Implementation steps

N/A (defer artifact; zero build time today). If a future v2 PR adopts the majia-huiyuan *BI + data-warehouse + membership + agent-skills + llm-friendly* primitive, the implementation steps would be:
1. Add a `daily_pnl` derived view in SQLModel that aggregates `StockEntry` by date.
2. Add a `membership_metrics` derived view in SQLModel that aggregates `CustomerVisit` (a future v2 table) by member_id.
3. Add a `coupon_batch` derived view in SQLModel that aggregates `Discount` (a future v2 table) by batch_id.
4. Add a `stored_value_analytics` derived view in SQLModel that aggregates `StoredValueTransaction` (a future v2 table) by member_id.
5. Add a `GET /analytics/daily-pnl` FastAPI endpoint that returns LLM-friendly JSON.
6. Add a `GET /analytics/membership/{member_id}` FastAPI endpoint that returns LLM-friendly JSON.
7. Test end-to-end: owner opens daily-P&L dashboard → sees today's revenue/cost/profit derived from `StockEntry`; owner asks AI assistant "what was yesterday's profit?" → AI assistant queries `GET /analytics/daily-pnl` and returns the LLM-friendly JSON.

## Telegram interaction

N/A (defer artifact; no Telegram surface change). The majia-huiyuan *BI + data-warehouse + membership + agent-skills + llm-friendly* primitive extends a *future v2 owner-analytics surface*, not the cook-bot surface; the cook-bot's `aiogram v3` Telegram-send flow is unchanged. (A v3 surface could potentially expose the daily-P&L via a Telegram `/pnl` command, but that's a separate trigger condition not in scope today.)

## Dependencies

**Existing LE31 v1 dependencies (unchanged):** Python 3.13, FastAPI, SQLModel, aiogram v3, uvicorn, Postgres.
**New v2 dependencies (if adopted):** BI framework (e.g. Apache Superset, Metabase) + LLM-friendly JSON serializer (e.g. `pydantic` with `model_dump_json()`) + optional v2 surface: `CustomerVisit` SQLModel table + `Discount` SQLModel table + `StoredValueTransaction` SQLModel table. All are optional v2 dependencies, not v1 dependencies.

## Open questions

- **Open question 1 (may kill any future PR)**: does LE31 v2 actually need an owner-analytics surface? The owner-analytics primitive is a *forward-looking* v2 surface that LE31 v2 may adopt when (or if) it introduces a *daily-P&L dashboard* or *membership-loyalty* surface; today LE31 v1 has no analytics surface.
- **Open question 2 (may kill any future PR)**: does LE31 v2 actually need a membership-loyalty surface? The membership-loyalty primitive is a *horizontal-expansion* surface that v2 may adopt; today LE31 v1 is *single-restaurant-vertical* per charter §3.1.
- **Open question 3 (may kill any future PR)**: does LE31 v2 actually need a LLM-friendly JSON export endpoint for owner AI-assist queries? The LLM-friendly primitive is a *forward-looking* v2 surface that LE31 v2 may adopt when (or if) it introduces an owner-assist AI surface; today LE31 v1 has no AI surface.
- **Open question 4 (architecture reference only)**: does LE31 v2 adopt majia-huiyuan's source code as a v2 dependency, or independently re-implement the *BI + data-warehouse + membership + agent-skills + llm-friendly* primitives against LE31's existing FastAPI + SQLModel + Postgres stack? Recommendation: independently re-implement (majia-huiyuan's *chinese + crm + guandata + workbuddy* surface is Chinese-market-CRM-specific; the *BI + data-warehouse + membership + agent-skills + llm-friendly* primitives are small enough to re-implement in <1000 LOC across derived-views + LLM-friendly-JSON-serializers + FastAPI endpoints).

## Why this matters

The **BI + data-warehouse + membership + restaurant + retail + agent-skills + llm-friendly + open-data + owner-tooling-AI-not-customer-facing-AI** nonuple-primitive is the **first v2-AI owner-analytics-vocabulary of the 58-pass series**. The *bi + data-warehouse* pair is the *business-intelligence + data-warehouse* pattern that LE31 v2 would need if (or when) it introduces a *daily-P&L + membership-analytics* surface (charter §3.1's *append-only-StockEntry-ledger* discipline maps directly onto a *data-warehouse-as-derived-tables-from-StockEntry* primitive where the `StockEntry` table is the *source-of-truth* and the *data-warehouse* tables are the *derived-views*). The *membership + restaurant + retail* triplet is the *membership-loyalty + restaurant + retail* vocabulary (LE31 v1 has no membership-loyalty surface today; the *membership-as-LE31-v2-horizontal-expansion-surface* discipline IS a v2 architectural option). The *agent-skills + llm-friendly* pair is the *AI-skill-as-package + LLM-friendly-data-shape* pattern that maps onto LE31 v2's charter §3.4 *AI-may-assist-owner-with-observable-evidence* posture. The *open-data* topic explicitly names the *open-data-posture* — the *data-warehouse-tables-are-open-to-the-owner-AI-assist* discipline. The majia-huiyuan pattern is the *future-v2-AI-architecture-vocabulary* for LE31 v2's owner-analytics-evolution.

**Fully reversible** (vocabulary-only artifact). Trigger condition = first v2-AI PR that introduces an owner-analytics surface (e.g. a `daily_pnl` derived view from `StockEntry` + a `membership_metrics` derived view from a future `CustomerVisit` table + a `coupon_batch` derived view from a future `Discount` table + a `stored_value_analytics` derived view from a future `StoredValueTransaction` table + LLM-friendly JSON export endpoints for owner AI-assist queries).
