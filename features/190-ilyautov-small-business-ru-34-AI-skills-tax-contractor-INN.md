# Feature 190 — `ilyautov-small-business-ru-34-AI-skills-tax-contractor-INN` (defer)

> **NEW observation (2026-09-17).** Documents in-window GitHub repo `ilyautov/small-business-ru` (**Apache-2.0 ✓**, **16★/4⑂**, Python, **pushed 2026-09-14T15:54:43Z (3 days ago, in-window by `pushed_at` only)**, **created 2026-06-15T21:29:32Z** (94-day-old repo with in-window push), **2051 KB** substantial repo, default_branch=`main`). Topics (verbatim from raw JSON): `accounting, agent-skills, ai, automation, chatgpt, claude, claude-code, claude-skills, codex, counterparty-check`. Description (verbatim, parent-verified GitHub API direct-GET 2026-09-17): *"34 открытых AI-скилла для малого бизнеса РФ: налоги УСН, деньги, проверка контрагента по ИНН. Числа считаются кодом, данные из реальных реестров. | 34 open AI skills for Russian small business: taxes, money, counterparty verification by INN. Numbers computed by code, data from real registries."* **The charter §3.4 invariant is explicitly named in the description: *"numbers computed by code, data from real registries"* — *the AI does not invent numbers; the AI invokes deterministic Python code + reads from real registries*. This is the exact charter §3.4 invariant language.** **Highest-star owner-assist AI-skill pattern of the 49-pass series.** Bucket: **v2-AI (operator-AI owner-assist, parking-lot defer)** — watch-list entry + charter §3.4 validation artifact, zero build time today.

## Goal

Retain the **"34 open AI skills for Russian small business: taxes, money, counterparty verification by INN; numbers computed by code, data from real registries"** cross-section architectural vocabulary as a persistent v2-AI owner-assist reference + **charter §3.4 invariant validation artifact** for the next LE31 v2 maintainer asking the *owner-assist AI-skill-as-package* question (does LE31 v2 introduce a discrete AI-skill package for owner-assist? if so, what is the charter §3.4 invariant pattern?). The artifact is the persistent *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* primitives as a *named* v2 architectural reference. No code today.

## Scope

**In scope (defer artifact):**
- A written record of the small-business-ru cross-section architectural vocabulary: the **highest-star owner-assist AI-skill pattern of the 49-pass series** (16★/4⑂); the Apache-2.0 license is charter §3.2-compatible; the `accounting + agent-skills + ai + automation + chatgpt + claude + claude-code + claude-skills + codex + counterparty-check` topic set maps 1:1 onto the LE31 charter §3.4 invariant.
- A written record of the **AI-skill-as-package primitive**: 34 discrete, packageable, owner-facing AI skills, each scoped to a single operator-pain (USN tax calculation, contractor verification by Russian INN registry, money tracking). The vocabulary is the named v2-architecture reference for any future LE31 AI-skill-as-package surface.
- A written record of the **charter §3.4 invariant language as explicit code principle**: *"Числа считаются кодом, данные из реальных реестров"* (numbers computed by code, data from real registries). The Russian-market focus is a *localization* detail; the *numbers-computed-by-code + data-from-real-registries* principle is portable to any LE31 v2 owner-assist surface. This is the **exact charter §3.4 invariant language**: *AI may assist owner/staff with observable evidence (the deterministic code + registry data) and non-AI fallback (the code works without the AI)*.
- A written record of the **deterministic-calculation + registry-data primitive**: the AI does not invent numbers; the AI invokes deterministic Python code + reads from real registries. The pattern is the operational implementation of charter §3.4.
- A cross-section reference with features `104-aldia-ai-agent-business-engine-cross-section` + `140-casedock-adhd-informed-solo-builder-workbench-htmx-design-posture` + `146-slashbooks-ai-bookkeeper-quickbooks-replacement-cross-section` + `158-azizsekerdil-smart-restaurant-management-system-ai-powered-pos-kds-inventory` — the same *AI-but-not-customer-facing / operator-must-confirm* cluster.
- A decision record: today's verdict is `defer (parking-lot)` because the *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* is a v2 architectural vocabulary + charter §3.4 validation, not a v1 build implication. v1 has no AI-assist surface today; v2 would extend the v1 primitives with these cluster primitives only if and when the v2 trigger condition fires.

**Out of scope (defer artifact):**
- Any change to the *no-cloud-no-AI* posture in v1 (charter §3.1 + §3.4). The small-business-ru repo uses an *explicit* AI-assist surface (Python + Claude Code + Codex + ChatGPT); LE31 v1 has no AI-assist surface.
- Any change to the cook Telegram bot surface.
- Adoption of the small-business-ru code as a v2 dependency (the repo is 16★/4⑂ at 94 days old; the *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* primitives would need to be independently re-implemented and validated against the LE31 append-only StockEntry ledger + audit_logs, not simply imported). The cross-section is *primitive vocabulary extension + charter §3.4 validation*, not *library adoption*.
- Any change to the `audit_logs` table or `StockEntry` ledger today.
- Russian-market localization: the *USN tax + INN registry* is a Russian-market detail; the *numbers-computed-by-code + data-from-real-registries* principle is portable. The Russian-market focus is NOT a v1 or v2 implementation detail.

## Evidence / JTBD

When a future LE31 v2 maintainer asks *"if v2 introduces an owner-assist AI surface, what is the charter §3.4 invariant pattern?"*, the maintainer wants *evidence that another independent 2026 single-maintainer Python repo at 16★ is shipping exactly the AI-skill-as-package + numbers-computed-by-code + data-from-real-registries primitives that LE31 v2 would need to satisfy charter §3.4*, but struggles because *v1 has no AI-assist surface and the charter §3.4 invariant is not yet operationalized in code*, so that *v2 can introduce the AI-assist surface with the explicit charter §3.4 invariant rather than inventing a new one*.

- **Evidence class**: observed (the small-business-ru repo description explicitly names *"numbers computed by code, data from real registries"* — exactly the charter §3.4 invariant language *"AI may assist owner/staff with observable evidence and non-AI fallback"*).
- **Confidence**: medium-high for the architectural vocabulary + charter §3.4 validation (16★/4⑂ is the highest in-window star count for any v2-AI owner-assist peer in 2026; Apache-2.0 + Python + 94-day-old + in-window push 3-days-ago confirms a real maintainer with sustained engagement; the *numbers-computed-by-code + data-from-real-registries* principle is explicitly named in the description). Confidence: low for *adoption* (LE31 should re-implement the *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* primitives against its own StockEntry + audit_logs, not import small-business-ru directly).
- **Real observed LE31 JTBD**: none directly. The cross-section is *primitive vocabulary extension + charter §3.4 validation*, not *LE31 demand*.
- **The value is primitive vocabulary extension + charter §3.4 validation**: when (if) LE31 v2 introduces the owner-assist AI surface, the *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* vocabulary is documented, and the charter §3.4 invariant is validated against an independent 2026 single-maintainer Python repo at 16★.

## LE31 seven-check gate verdict

| Check | Verdict |
|---|---|
| 1. Raison d'être / JTBD | Inferred — the architectural vocabulary + charter §3.4 validation is real but no LE31 owner has asked for *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* in 49 passes. The owner today uses feature 104 + 140 + 146 + 158 cluster-watch entries to track the AI-but-not-customer-facing / operator-must-confirm convergence. |
| 2. Viability | Mechanism is well-defined (Python + Claude Code + Codex + ChatGPT); viability is conditional on a v2 owner-facing AI-assist question. |
| 3. Practicability and confidence | Confidence: medium-high for the architectural vocabulary + charter §3.4 validation (16★/4⑂ is the highest in-window star count for any v2-AI owner-assist peer in 2026; Apache-2.0 + Python + 94-day-old + in-window push 3-days-ago confirms real maintainer with sustained engagement; the *numbers-computed-by-code + data-from-real-registries* principle is explicitly named in the description). Stack: on-pattern (Python matches LE31; Claude Code + Codex + ChatGPT are v2-AI surface primitives, not v1 stack primitives). Practicability of adoption: medium — the *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* primitives can be extracted and re-implemented against LE31's StockEntry + audit_logs. |
| 4. Conflict | No conflict with charter §3.1 (the *numbers-computed-by-code + data-from-real-registries* posture is explicitly §3.1-aligned: deterministic state transitions + observable evidence); §3.2 (Apache-2.0 = permissive); §3.4 (the small-business-ru repo is an operator-facing AI-skill package, not customer-facing — the *numbers-computed-by-code + data-from-real-registries* principle is the explicit enforcement boundary that excludes customer-facing AI per charter §3.4). Charter §3.4 explicitly validated. |
| 5. Outcome, appetite, scope | v2-AI owner-assist (cross-section architectural vocabulary + charter §3.4 validation). Appetite: zero today. |
| 6. Cost to operational value | Zero cost today; zero value today. Future contingency value: medium-high (the *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* vocabulary + charter §3.4 validation is the named architectural reference for any future v2 owner-assist AI surface). |
| 7. Circuit breaker and reversibility | Trivially reversible: written reference, no code. |

**Decision: defer (parking-lot).** No v1 pain; no v2 build implication today. The 16★/4⑂ + Apache-2.0 + 94-day-old + Python + in-window push 3-days-ago + *numbers-computed-by-code + data-from-real-registries* explicit-principle combo is a *novelty signal* + *vocabulary-validation signal* + *charter §3.4 invariant validation artifact*, not a *credibility signal*; the architectural vocabulary + charter §3.4 validation is the value.

## Implementation steps

None today. When v2 maintainer asks the v2 owner-assist AI question, the *architectural vocabulary validation* to surface is **"another independent 2026 single-maintainer Python repo at 16★ is shipping exactly the AI-skill-as-package + numbers-computed-by-code + data-from-real-registries primitives that LE31 v2 would need to satisfy charter §3.4"** — and the *charter §3.4 validation* to surface is **"the explicit *numbers-computed-by-code + data-from-real-registries* principle is the operational implementation of the charter §3.4 invariant: AI may assist owner/staff with observable evidence (the deterministic code + registry data) and non-AI fallback (the code works without the AI)"**. This is a *primitive vocabulary extension + charter §3.4 validation*, not a v1 build implication; v2 maintainer decision required before any v2 surface adopts the *AI-skill-as-package + numbers-computed-by-code + data-from-real-registries* posture.

## Dependencies

- None today.
- Future dependencies (v2 only): a v2 maintainer decision on whether to introduce an owner-assist AI surface; an *AI-skill-as-package* design that adopts the *numbers-computed-by-code + data-from-real-registries* primitives; an *AI-assist risk-model* that explicitly enforces the *numbers-computed-by-code + data-from-real-registries* principle per charter §3.4. None of these are in v1 scope.

## Open questions

1. **Does the small-business-ru *34 AI skills* package map to discrete Python skills (Claude Code skills / Codex skills) or to a single multi-purpose prompt?** Today: unknown — repo source not yet read. If discrete Python skills, the cross-section is *AI-skill-as-package primitive*; if single multi-purpose prompt, the cross-section is *JTBD validation only*. Recommend read-and-cite on next v2-architecture-review moment.
2. **Does the small-business-ru *numbers-computed-by-code + data-from-real-registries* principle include explicit verification (the code returns the same answer twice) or just descriptive documentation?** Today: unknown. If explicit verification, the cross-section is *charter §3.4 invariant validation with verifiable evidence*; if descriptive documentation, the cross-section is *charter §3.4 invariant validation with descriptive evidence*. Recommend read-and-cite.
3. **Is the small-business-ru *USN tax + INN registry* a Russian-market-specific primitive or a portable *deterministic-calc + real-registry* pattern?** Today: Russian-market-specific surface; portable pattern. The *USN tax + INN registry* is the Russian-market implementation; the *deterministic-calc + real-registry* is the portable pattern. LE31 v2 would adopt the portable pattern with LE31-specific real-registry sources (e.g., French `tva` + `siret` registries, US `ein` + `sales-tax` registries).
4. **Is the small-business-ru repo actively maintained?** Today: 16★/4⑂/94-day-old + Apache-2.0 + in-window push 3-days-ago = active-maintainer signal. Recommend re-check at the next 7-day boundary.

## Why this matters

The small-business-ru repo is the **first in-window v2-AI owner-assist peer of the 49-pass series at 16★** + Apache-2.0 + 94-day-old + in-window push 3-days-ago + **explicit charter §3.4 invariant language** (*"numbers computed by code, data from real registries"*) in the description. The *architectural vocabulary + charter §3.4 validation* — *"the AI-skill-as-package + numbers-computed-by-code + data-from-real-registries pattern is the named 2026 pattern for any future LE31 v2 owner-assist AI surface that adopts the charter §3.4 invariant"* — is the **highest-star owner-assist AI-skill pattern of the 49-pass series**. For LE31 v1, the implication is *none* (v1 has no AI-assist surface). For LE31 v2, the implication is *the AI-skill-as-package + numbers-computed-by-code + data-from-real-registries vocabulary is documented, and the charter §3.4 invariant is validated against an independent 2026 single-maintainer Python repo at 16★*. **No build today.**

## Cross-section (vs the prior 17-pick cluster)

- Feature 104 (`aldia-ai-agent-business-engine-cross-section`) — *sister-shape* (GitHub `jonalemndi2/ALdia` — Apache-2.0, 5★, Python, business engine for AI agents with invoicing + payments + inventory + idempotent MCP tools; small-business-ru is the highest-star AI-skill-as-package peer in 2026).
- Feature 140 (`casedock-adhd-informed-solo-builder-workbench-htmx-design-posture`) — *sister-shape* (GitHub `gerpaick/casedock` — Apache-2.0, 2★, Django + HTMX, calm workbench for solo technical builders; small-business-ru is the highest-star operator-must-confirm + numbers-from-real-registries peer).
- Feature 146 (`slashbooks-ai-bookkeeper-quickbooks-replacement-cross-section`) — *sister-shape* (GitHub `giltotherescue/slashbooks` — MIT, 1★, Python, AI bookkeeper QuickBooks replacement; small-business-ru is the highest-star AI-skill-as-package + numbers-from-real-registries peer).
- Feature 158 (`azizsekerdil-smart-restaurant-management-system-ai-powered-pos-kds-inventory`) — *sister-shape* (GitHub `AzizSeker/smart-restaurant-management-system` — MIT, 1★, Python, AI-powered restaurant management system; small-business-ru is the highest-star AI-skill-as-package peer outside the restaurant domain).
- 09-16 cluster (`Aspct3434/Distill-Agent` + `OtakuNathan/Pal` + `nemuprojectofficial-glitch/n0-public` — the latter filed as feature 183) — *sister-shape cluster* (the v2-AI control-plane + deterministic-gate + append-only-ledger pattern; small-business-ru is the highest-star operator-must-confirm + numbers-from-real-registries peer at 16★ vs ≤3★ for the 09-16 cluster).

The *transferable insight* is **the AI-skill-as-package + numbers-computed-by-code + data-from-real-registries architectural vocabulary** — and the **charter §3.4 invariant validation artifact** — *the AI-may-assist-with-observable-evidence-and-non-AI-fallback posture is the named 2026 pattern for any operator-assist AI surface, and LE31 v1 already implements the *numbers-from-code* posture (LE31's `audit_logs` records explicit code-driven state transitions) but does NOT yet implement the *AI-skill-as-package + data-from-real-registries* posture* (v2 trigger condition).
