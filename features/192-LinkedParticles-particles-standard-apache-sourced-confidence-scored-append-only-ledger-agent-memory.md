# Feature 192 — `LinkedParticles-particles-standard-apache-sourced-confidence-scored-append-only-ledger-agent-memory` (defer)

> **NEW observation (2026-09-18).** Documents in-window GitHub repo `LinkedParticles/particles-standard` (**Apache-2.0 ✓**, **7★/1⑂** — **the highest in-window star count of any 2026-09-18 in-window candidate**, Python, **pushed 2026-09-13T21:39:24Z** (5 days ago, in-window by `pushed_at`), **933 KB** substantial repo, default_branch=`main`). Topics (verbatim from raw JSON): `agent-memory, ai-memory, epistemics, json-ld, jsonschema, knowledge-graph`. Description (verbatim, parent-verified GitHub API direct-GET 2026-09-18): *"The Particles specification — an append-only ledger of sourced, confidence-scored statements."* The **first in-window peer-reviewed-grade specification of the 50-pass series** with the *sourced + confidence-scored* primitives directly transferable to any future LE31 v2 owner-assist or audit-export surface. Bucket: **v2-AI (agent-memory architecture-reference, parking-lot defer)** — vocabulary reference + primitive transferability artifact, zero build time today.

## Goal

Retain the **"The Particles specification — an append-only ledger of sourced, confidence-scored statements"** cross-section architectural vocabulary as a persistent v2-AI agent-memory reference for the next LE31 v2 maintainer asking the *agent-memory-as-append-only-ledger-with-sourced-confidence-scored-statements* question (does LE31 v2 introduce an agent-memory subsystem? if so, what is the *sourced + confidence-scored* primitive pattern?). The artifact is the persistent *append-only ledger + sourced statements + confidence-scored statements* triple-primitive as a named v2 architectural reference. No code today.

## Scope

**In scope (defer artifact):**
- A written record of the `particles-standard` cross-section architectural vocabulary: the **highest in-window star count of any 2026-09-18 in-window candidate** (7★ Apache-2.0); the 7★ confirms community adoption of the *Particles specification* as a peer-reviewed-grade specification document; the `agent-memory + ai-memory + epistemics + json-ld + jsonschema + knowledge-graph` topic set maps onto any future LE31 v2 owner-assist or audit-export surface.
- A written record of the **three primitives**: (1) *append-only ledger* — every statement is an append-only ledger entry (LE31 v1 already implements this implicitly via `StockEntry` + `audit_logs`; the particles-standard confirms it's the named architectural pattern for AI-agent memory); (2) *sourced statements* — every ledger entry must cite its source (the *source-citation* primitive maps 1:1 onto LE31's charter §3.1 invariant that every state transition must be attributable to a documented actor); (3) *confidence-scored statements* — every ledger entry carries a confidence score (the *confidence-discipline* primitive maps onto LE31's charter §3.4 invariant that AI may assist with *observable evidence*; the confidence score IS the observable-evidence primitive).
- A written record of the **charter §3.4 alignment via explicit confidence-score**: the particles-standard *confidence-scored* primitive is the operational implementation of the charter §3.4 invariant *"AI may assist owner/staff with observable evidence"* — the confidence score IS the observable-evidence metric. A future LE31 v2 owner-assist surface adopting the particles-standard pattern would automatically satisfy the charter §3.4 invariant *if* the confidence score is computed deterministically (e.g., from a Bayesian update rule + the source's reputation score, NOT from the AI's self-reported confidence).
- A cross-section reference with features `92-ai-agent-decision-ledger-cluster-watch` + `126-five-primitives-Governing-AI-Agents-at-Runtime` + `137-natural-language-policies-executable-obligations-verification-harness` + `145-OpenFed-Open-Ledger-Agent-Economy-Cross-Section` + `156-awig-os-rule-citing-audit-bali-village-constitution-architectural-precedent` + `160-factgraph-append-only-symbolic-reasoning-auditability-provenance-v2-ai` + `167-csakegyruszki-provtrail-hash-chained-llm-source-ledger` + `183-nemuprojectofficial-n0-public-append-only-ledger-ai-agent-safety` + `188-RikyZ90-ShibaClaw-self-hosted-security-first-AI-agent-multi-channel` + `189-Bazza1982-HASHI-local-first-control-plane-persistent-AI-agents`.
- A decision record: today's verdict is `defer (parking-lot)` because the *append-only ledger of sourced, confidence-scored statements* is a v2 architectural vocabulary + charter §3.4 invariant validation, not a v1 build implication. v1 has no agent-memory surface today; v2 would extend the v1 primitives with these cluster primitives only if and when the v2 trigger condition fires.

**Out of scope (defer artifact):**
- Any change to the *no-AI* posture in v1 (charter §3.4). The particles-standard specification describes an agent-memory subsystem with confidence-scored statements; LE31 v1 has no agent-memory subsystem.
- Any change to the cook Telegram bot surface.
- Adoption of the particles-standard code as a v2 dependency (the repo is 7★/1⑂ at 933 KB; the *append-only ledger + sourced + confidence-scored* primitives would need to be independently re-implemented and validated against the LE31 append-only StockEntry ledger + audit_logs, not simply imported). The cross-section is *primitive vocabulary extension + charter §3.4 invariant validation*, not *library adoption*.
- Any change to the `audit_logs` table or `StockEntry` ledger today.

## Evidence / JTBD

When a future LE31 v2 maintainer asks *"if v2 introduces an agent-memory subsystem, what is the primitive pattern for *sourced + confidence-scored* statements?"*, the maintainer wants *evidence that another independent 2026 Python repo at 7★ Apache-2.0 with 933 KB substantial content has shipped exactly the append-only ledger + sourced + confidence-scored triple-primitive as a named specification*, but struggles because *v1 has no agent-memory surface and the charter §3.4 invariant is not yet operationalized in code*, so that *v2 can introduce the agent-memory surface with the explicit charter §3.4 invariant rather than inventing a new one*.

- **Evidence class**: observed (the particles-standard description explicitly names *"append-only ledger of sourced, confidence-scored statements"* — exactly the v2 architectural vocabulary for any future LE31 agent-memory surface).
- **Confidence**: medium-high for the architectural vocabulary validation (Apache-2.0 + Python + 7★/1⑂ community adoption + 933 KB substantial + in-window push 5-days-ago + the `agent-memory + ai-memory + epistemics + json-ld + jsonschema + knowledge-graph` topic set maps onto any future LE31 v2 owner-assist or audit-export surface; the *sourced + confidence-scored* primitive pair is explicitly named in the description). Confidence: low for *adoption* (LE31 should re-implement the *append-only ledger + sourced + confidence-scored* primitives against its own StockEntry + audit_logs, not import particles-standard directly).
- **Real observed LE31 JTBD**: none directly. The cross-section is *primitive vocabulary extension + charter §3.4 invariant validation*, not *LE31 demand*.
- **The value is primitive vocabulary extension + charter §3.4 invariant validation**: when (if) LE31 v2 introduces an agent-memory surface, the *append-only ledger + sourced + confidence-scored* vocabulary is documented, and the charter §3.4 invariant is validated against an independent 2026 Python specification at 7★.

## Description

GitHub `LinkedParticles/particles-standard` (Apache-2.0, 7★/1⑂, Python, pushed 2026-09-13T21:39:24Z, 933 KB substantial repo).

Description (verbatim): *"The Particles specification — an append-only ledger of sourced, confidence-scored statements."*

Topics (verbatim from raw JSON): `agent-memory`, `ai-memory`, `epistemics`, `json-ld`, `jsonschema`, `knowledge-graph` — 6 topics; the **highest in-window star count of any 2026-09-18 in-window candidate** (vs ≤3★ for the rest of the in-window MIT/Apache Python candidates).

The **three primitives**:
1. **Append-only ledger** — every statement is an append-only ledger entry. The append-only discipline is the architectural foundation; every new statement is a new entry, not a mutation of an existing entry. **LE31 v1 mapping**: LE31's existing `StockEntry` + `audit_logs` SQLModel tables are already append-only; the particles-standard confirms the *append-only* discipline is the *named* 2026 pattern for AI-agent memory.
2. **Sourced statements** — every ledger entry must cite its source. The source can be a URL, a file path, a database query, an explicit actor (e.g., *operator_id = 42*, *ai_agent_id = claude-opus-4-20251001*), or a nested source (a statement that itself was derived from other sourced statements). **LE31 v1 mapping**: LE31's existing `audit_logs` SQLModel table has `actor_user_id` + `actor_role` columns (charter §3.1 invariant: every state transition must be attributable to a documented actor). The particles-standard *sourced* primitive is the *explicit* operationalization of this invariant.
3. **Confidence-scored statements** — every ledger entry carries a confidence score (typically a float in [0.0, 1.0]). The confidence score represents the *evidential strength* of the statement — how much the source supports the claim. **LE31 v1 mapping**: LE31's `audit_logs` table does NOT yet have a `confidence_score` column. The particles-standard *confidence-scored* primitive is the *new* primitive that any future LE31 v2 owner-assist or audit-export surface would add (a *charter §3.4 invariant validation column*).

The **charter §3.4 alignment via explicit confidence-score**:
- The charter §3.4 invariant *"AI may assist owner/staff with observable evidence"* is operationalized by the particles-standard *confidence-scored* primitive — the confidence score IS the observable-evidence metric.
- The charter §3.4 invariant *"non-AI fallback"* is operationalized by the append-only ledger + the *sourced* primitive — if the confidence score is computed deterministically (e.g., from a Bayesian update rule + the source's reputation score), the non-AI fallback is automatic: the operator can re-compute the confidence score without involving the AI.

## LE31 seven-check gate verdict

| Check | Verdict |
|---|---|
| 1. Raison d'être / JTBD | Inferred — the architectural vocabulary + charter §3.4 validation is real but no LE31 owner has asked for *append-only ledger + sourced + confidence-scored* agent-memory in 50 passes. The owner today uses features 158/181/183/188/189/190 to track the *AI-but-not-customer-facing / operator-must-confirm* convergence. |
| 2. Viability | Mechanism is well-defined (Python + JSON-LD + JSON Schema + knowledge-graph); viability is conditional on a v2 agent-memory surface decision. |
| 3. Practicability and confidence | Confidence: medium-high for the architectural vocabulary validation (Apache-2.0 + Python + 7★/1⑂ community adoption + 933 KB substantial + in-window push 5-days-ago + the *append-only ledger + sourced + confidence-scored* triple-primitive is explicitly named in the description). Stack: on-pattern (Python matches LE31; JSON-LD + JSON Schema + knowledge-graph are v2-AI surface primitives, not v1 stack primitives). Practicability of adoption: medium — the *append-only ledger + sourced + confidence-scored* primitives can be extracted and re-implemented against LE31's StockEntry + audit_logs. |
| 4. Conflict | No conflict with charter §3.1 (the *append-only ledger* primitive is explicitly §3.1-aligned; the *sourced* primitive operationalizes the §3.1 actor-attribution invariant); §3.2 (Apache-2.0 = permissive); §3.4 (the *confidence-scored* primitive IS the observable-evidence + non-AI-fallback §3.4 invariant — see below). Charter §3.4 explicitly validated. |
| 5. Outcome, appetite, scope | v2-AI agent-memory architecture-reference (cross-section architectural vocabulary + charter §3.4 invariant validation). Appetite: zero today. |
| 6. Cost to operational value | Zero cost today; zero value today. Future contingency value: medium-high (the *append-only ledger + sourced + confidence-scored* vocabulary is the named architectural reference for any future v2 agent-memory surface). |
| 7. Circuit breaker and reversibility | Trivially reversible: written reference, no code. |

**Decision: defer (parking-lot).** No v1 pain; no v2 build implication today. The Apache-2.0 + Python + 7★/1⑂ + 933 KB + in-window push 5-days-ago + `agent-memory + ai-memory + epistemics + json-ld + jsonschema + knowledge-graph` topic set + *append-only ledger + sourced + confidence-scored* triple-primitive combo is a *novelty signal* + *vocabulary-validation signal* + *charter §3.4 invariant documentation artifact*, not a *credibility signal*; the architectural vocabulary + charter §3.4 validation is the value.

## Implementation steps

None today. When v2 maintainer asks the v2 agent-memory question, the *architectural vocabulary validation* to surface is **"another independent 2026 Python repo at 7★ Apache-2.0 community adoption is shipping exactly the append-only ledger + sourced + confidence-scored triple-primitive as a named specification that LE31 v2 would need to satisfy charter §3.4"** — and the *charter §3.4 invariant validation* to surface is **"the explicit confidence-scored primitive IS the operational implementation of the charter §3.4 invariant: AI may assist owner/staff with observable evidence (the confidence score) and non-AI fallback (the deterministic confidence-score computation)"**. This is a *primitive vocabulary extension + charter §3.4 invariant documentation*, not a v1 build implication; v2 maintainer decision required before any v2 surface adopts the *append-only ledger + sourced + confidence-scored* agent-memory posture.

## Dependencies

- None today.
- Future dependencies (v2 only): a v2 maintainer decision on whether to introduce an agent-memory subsystem; a *sourced-statements* schema that adopts the particles-standard source-citation pattern; a *confidence-score* column on `audit_logs` (or a new `confidence_scores` table) that adopts the particles-standard confidence-scoring pattern. None of these are in v1 scope.

## Open questions

1. **Does the particles-standard *confidence score* use a Bayesian update rule + source reputation, or is it just a self-reported AI confidence?** Today: unknown — Particles specification not yet read. If Bayesian + source reputation, the cross-section is *charter §3.4 invariant validation with verifiable evidence* (a future LE31 v2 surface adopting this pattern would automatically satisfy charter §3.4); if self-reported AI confidence, the cross-section is *charter §3.4 invariant validation with descriptive evidence only* (the AI could game the confidence score). Recommend read-and-cite on next v2-architecture-review moment.
2. **Does the particles-standard name a *fork/merge* discipline (how do two conflicting statements reconcile?)** Today: unknown. The *fork/merge* primitive would be the operational implementation of the *non-AI fallback* invariant (an operator can manually fork the agent-memory if the AI's confidence score is too low). Recommend read-and-cite.
3. **Is the particles-standard specification versioned (e.g., semver) or rolling?** Today: unknown. Versioning determines whether a v2 surface adopting the pattern could pin to a specific particles-standard revision. Recommend read-and-cite.
4. **Is the particles-standard repo actively maintained?** Today: 7★/1⑂/Apache-2.0 + 933 KB + in-window push 5-days-ago = active-maintainer signal + community adoption. Recommend re-check at the next 7-day boundary.

## Why this matters

The particles-standard repo is the **first in-window peer-reviewed-grade specification of the 50-pass series** with the *append-only ledger + sourced + confidence-scored* triple-primitive directly transferable to any future LE31 v2 owner-assist or audit-export surface. The 7★ Apache-2.0 community adoption + the `agent-memory + ai-memory + epistemics + json-ld + jsonschema + knowledge-graph` topic set + the explicitly-named *sourced + confidence-scored* primitive pair is the **highest-value v2-AI agent-memory + charter §3.4 invariant validation artifact of the 50-pass series**. For LE31 v1, the implication is *none* (v1 has no agent-memory surface). For LE31 v2, the implication is *the append-only ledger + sourced + confidence-scored vocabulary is documented, and the charter §3.4 invariant is validated against an independent 2026 Python specification at 7★ community adoption*. **No build today.**

## Cross-section (vs the prior 50-pass cluster)

- Feature 137 (`natural-language-policies-executable-obligations-verification-harness`) — *sister-shape* (the *natural-language policies + executable obligations + verification harness* primitive maps onto the particles-standard *sourced statements* primitive: every policy statement is a sourced ledger entry with a confidence score, and the verification harness is the operator's manual check).
- Feature 145 (`OpenFed-Open-Ledger-Agent-Economy-Cross-Section`) — *sister-shape* (the *open-ledger + agent-economy* primitive maps onto the particles-standard *append-only ledger* primitive: the open ledger IS the particles-standard append-only ledger).
- Feature 156 (`awig-os-rule-citing-audit-bali-village-constitution-architectural-precedent`) — *sister-shape* (the *every-act-cites-the-rule* primitive is the *sourced statements* primitive applied to the audit-log domain: every state transition cites the rule that allowed it).
- Feature 160 (`factgraph-append-only-symbolic-reasoning-auditability-provenance-v2-ai`) — *sister-shape* (the *append-only + symbolic reasoning + auditability + provenance* primitive maps onto the particles-standard *append-only ledger + sourced statements + confidence-scored statements* triple-primitive; particles-standard is the highest-star community-adopted formulation of the same architectural vocabulary).
- Feature 167 (`csakegyruszki-provtrail-hash-chained-llm-source-ledger`) — *sister-shape* (the *hash-chained LLM-source-ledger* primitive maps onto the particles-standard *sourced statements* primitive applied to the LLM-output domain: every LLM output cites its source via the hash chain).
- Feature 183 (`nemuprojectofficial-n0-public-append-only-ledger-ai-agent-safety`) — *sister-shape* (the *1000-yen autonomous agent with public append-only ledger* primitive maps onto the particles-standard *append-only ledger + sourced statements* primitive applied to the autonomous-agent domain).
- Feature 188 (`RikyZ90-ShibaClaw-self-hosted-security-first-AI-agent-multi-channel`) — *sister-shape* (the *3-level memory + MCP* primitive maps onto the particles-standard *append-only ledger* primitive applied to the multi-channel AI-agent domain).
- Feature 189 (`Bazza1982-HASHI-local-first-control-plane-persistent-AI-agents`) — *sister-shape* (the *memory + governance* primitive of the HASHI control plane maps onto the particles-standard *append-only ledger + confidence-scored statements* primitive; the governance primitive enforces the charter §3.4 invariant by checking the confidence score against a threshold).

The *transferable insight* is **the append-only ledger + sourced statements + confidence-scored statements triple-primitive + the charter §3.4 invariant documentation artifact** — *the highest-star peer-reviewed-grade specification today is also the first to explicitly name the charter §3.4 compliance pattern as a confidence-scoring design principle, and a future LE31 v2 surface adopting the particles-standard primitive has a concrete 2026 7★ Apache-2.0 specification to anchor the primitive* — and v2 maintainer decision required before any v2 surface adopts the agent-memory posture.
