# Feature 216 — `bangnevgo-ai-workflow-os-append-only-ledger-5-state-machine-tamper-evident-jsonl-evidence-gated-done-zero-dependency` (defer)

> **NEW observation (2026-09-22).** Documents in-window GitHub Search `topic:append-only` query result: `bangnevgo/ai-workflow-os` (**0★/0⑂**, **MIT ✓**, Python, **pushed 2026-09-04T20:34:14Z = 18 days ago** (in-window by `pushed_at`) + **created 2026-09-04T19:51:03Z = 18 days ago** (in-window by `created_at` — **BOTH fields in-window**; *both fields fall in the same ~30-minute window — created_at 2026-09-04T19:51:03Z + pushed_at 2026-09-04T20:34:14Z = ~43 minutes apart*; the **only of today's 3 picks where both fields fall in the same ~30-minute window**), **1119 KB substantial repo**, default_branch=`main`, archived=`False`, `updated_at=2026-09-04T20:34:19Z`). **17 topics** (parent-verified count; subagent claimed 16 — parent re-counted raw JSON `topics` array = 17 = **off-by-one error recorded honestly**): `agent-memory`, `agent-orchestration`, `ai-agents`, `append-only`, `claude`, `claude-agents`, `developer-tools`, `jsonl`, `karpathy-wiki`, `ledger`, `llm`, `multi-agent`, `operating-system`, `python`, `state-machine`, `workflow`, `workflow-automation`. Description (verbatim, parent-verified GitHub API direct-GET 2026-09-22): *"Append-only ledger + 5-state machine that keeps 6 divisions honest across 5 AI platforms (Claude, ChatGPT, Gemini). CLI for LLM agent workflow tracking: evidence-gated 'done', dependency holds, tamper-evident JSONL history, and Memory & Dreaming (Karpathy wiki pattern) cross-session memory for AI agents. Zero-dependency Python."*. The **append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python** septuple primitive — the *append-only + ledger + state-machine* triple IS the *charter §3.2 StockEntry-append-only + charter §3.1 explicit-state-transition discipline by construction* (every state transition writes a new row in the append-only ledger; every row is an event; every event is explicit — no silent transition; the entity's current state is derived from the rows). The **strongest in-window 2026 sister-shape to feature 3 (`kitchen-stock-tracker` charter §3.2 baseline) the 54-pass brainstorm series has surfaced**. Charter §3.1 alignment (the *append-only-ledger + 5-state-machine + evidence-gated-done + dependency-holds* discipline IS the *charter §3.1 explicit-state-transition discipline + charter §3.2 StockEntry-append-only discipline by construction*); §3.2 STRICTLY-COMPATIBLE (MIT permissive + zero-dependency Python = matches LE31's *lean-stack* posture); §3.4 NOT triggered (the AI surface is *operator-tooling AI* for cross-session memory + workflow tracking, not customer-facing AI; same posture as features 119 + 125 + 129 + 134 + 144 + 199 + 203).

## Goal

Retain the **append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python** cross-section architectural vocabulary + **the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history* triple + **the *evidence-gated-done + dependency-holds* discipline + **the *Karpathy-wiki-pattern cross-session-memory* primitive + **the *zero-dependency-Python* posture** as the persistent v1 append-only-ledger state-machine reference for any future LE31 v1 or v2 maintainer asking the *append-only-ledger + state-machine + tamper-evident-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern* question (does LE31 v1 ever extend the `StockEntry` append-only ledger with a state-machine discipline? does LE31 v2 ever introduce an evidence-gated-done discipline? does LE31 v2 ever introduce a dependency-holds discipline? does LE31 v2 ever introduce a Karpathy-wiki-pattern cross-session-memory primitive?). The artifact is the persistent *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python* vocabulary as a *named* v1 append-only-ledger state-machine reference. No code today.

## Scope

**In scope (defer artifact):**
- A written record of the `bangnevgo/ai-workflow-os` cross-section vocabulary: the **strongest in-window 2026 sister-shape to feature 3 (`kitchen-stock-tracker` charter §3.2 baseline) the 54-pass brainstorm series has surfaced**; the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python* septuple primitive explicitly named in the description.
- A written record of the **append-only-ledger primitive**: the *append-only-ledger* discipline IS the *charter §3.2 StockEntry-append-only discipline by construction* (every state transition writes a new row in the append-only ledger; no row is ever updated or deleted; the entity's current state is derived from the rows; the ledger is the source of truth).
- A written record of the **5-state-machine primitive**: the *5-state-machine* discipline IS the *charter §3.1 explicit-state-transition discipline by construction* (the entity has 5 named states; every transition is from one named state to another named state; the state-machine validates that every transition is legal; no silent transition; the entity cannot move to an undefined state).
- A written record of the **tamper-evident-JSONL-history primitive**: the *tamper-evident-JSONL-history* discipline IS the *charter §3.2 audit-log-integrity discipline by construction* (every event is written as a JSONL row; the *tamper-evident* qualifier = the history-cannot-be-tampered-with discipline; every row is hashed + chained; a tampered row breaks the hash chain; the chain is verifiable on demand).
- A written record of the **evidence-gated-done discipline**: an entity is not *done* until an evidence file is written; the *evidence-file* is the *evidence* that the work was done; the *evidence-gate* is the *gate* that prevents premature marking-as-done; the *evidence-gate* discipline IS the *charter §3.1 explicit-state-transition discipline applied to the done-state dimension* (an entity cannot move to *done* until an evidence file is written; the move requires explicit evidence; no silent transition).
- A written record of the **dependency-holds discipline**: an entity cannot transition to *done* until all dependencies are satisfied; the *dependency* is the *prerequisite*; the *hold* is the *gate* that prevents premature transition when dependencies are unmet; the *dependency-holds* discipline IS the *charter §3.1 explicit-state-transition discipline applied to the dependency dimension* (an entity cannot move to a downstream state until all upstream dependencies are satisfied).
- A written record of the **Karpathy-wiki-pattern cross-session-memory primitive**: the *Karpathy-wiki-pattern* is Andrej Karpathy's *wiki* pattern for cross-session memory (the AI writes notes to its own wiki; the wiki is the cross-session memory; the *dreaming* pattern = periodically the AI reviews its own wiki + consolidates memories); the *Karpathy-wiki-pattern* discipline IS the *charter §3.4 observable-evidence discipline applied to the cross-session-memory dimension* (the wiki is the evidence; the wiki is observable; the AI cannot silently rewrite its own memory).
- A written record of the **zero-dependency-Python posture**: the codebase uses only the Python standard library (no pip install needed; no requirements.txt); the *zero-dependency* posture matches LE31 v1's *lean-stack* posture (LE31 v1 uses FastAPI + SQLModel + aiogram v3 + Postgres — a minimal set of well-known dependencies).
- A written record of the **MIT permissive license as §3.2 STRICTLY-COMPATIBLE**: the codebase CAN be imported as a v2 dependency under MIT permissive licensing (no copyleft restrictions, no source-disclosure requirements); the zero-dependency posture means no transitive dependency conflicts.
- A decision record: today's verdict is `defer (parking-lot)` because the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python* vocabulary is a v1 append-only-ledger state-machine reference, not a v1 or v2 build implication. v1 has no state-machine extension to `StockEntry` today; the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern* vocabulary documents the *future-extension* for the next v1 or v2 maintainer asking the *append-only-ledger + state-machine + tamper-evident-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern* question.

**Out of scope (defer artifact):**
- Any change to the LE31 v1 surface (no state-machine extension to `StockEntry` today, no evidence-gated-done today, no dependency-holds today, no Karpathy-wiki-pattern cross-session-memory today).
- Any change to the LE31 v1 cook Telegram bot (no Karpathy-wiki-pattern cross-session-memory today; the cook-bot is purely transactional).
- Any change to the LE31 v1 waiter web UI (no evidence-gated-done today; the waiter UI is purely transactional).
- Adoption of the `bangnevgo/ai-workflow-os` code as a v2 dependency (the repo is 0★/0⑂ at 1119 KB with MIT permissive license; the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python* vocabulary would need to be independently re-implemented and validated against the LE31 v1's no-state-machine-extension-to-StockEntry + no-evidence-gated-done + no-dependency-holds + no-Karpathy-wiki-pattern posture, not simply imported; vocabulary-only artifact today).
- Any change to the `audit_logs` table or `StockEntry` ledger today.
- Any new dependency on the `bangnevgo` maintainer.

## Evidence / JTBD

When a future LE31 v1 or v2 maintainer asks *"if v1 or v2 extends the `StockEntry` append-only ledger with a state-machine discipline, an evidence-gated-done discipline, a dependency-holds discipline, or a Karpathy-wiki-pattern cross-session-memory primitive, what is the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python* vocabulary that preserves the existing v1 StockEntry-append-only + no-state-machine-extension + no-evidence-gated-done + no-dependency-holds + no-Karpathy-wiki-pattern posture?"*, the maintainer wants *evidence that another independent 2026 Python repo at 1119 KB with 0★/0⑂ + MIT permissive license + 17 topics + both `pushed_at` AND `created_at` in-window is shipping the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python* primitive as the v1 append-only-ledger state-machine discipline*, but struggles because *v1 has no documented state-machine-extension-to-StockEntry primitive in the charter*, so that *v1 or v2 can introduce the state-machine-extension-to-StockEntry + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern vocabulary with the explicit charter §3.1 + §3.2 discipline rather than reinventing the pattern*.

- **Evidence class**: observed (the `bangnevgo/ai-workflow-os` description explicitly names *"Append-only ledger + 5-state machine that keeps 6 divisions honest across 5 AI platforms (Claude, ChatGPT, Gemini). CLI for LLM agent workflow tracking: evidence-gated 'done', dependency holds, tamper-evident JSONL history, and Memory & Dreaming (Karpathy wiki pattern) cross-session memory for AI agents. Zero-dependency Python."* = the append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern + zero-dependency-Python septuple primitive).
- **Confidence**: high for the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern + zero-dependency-Python* vocabulary (0★/0⑂ + MIT permissive license + 17 topics + both fields in-window + description verbatim = §3.2 STRICTLY-COMPATIBLE for vocabulary + architecture-reference adoption; the *append-only + ledger + state-machine + jsonl* 4-LE31-relevant-topic cluster = the strongest in-window 2026 sister-shape to feature 3 the 54-pass series has surfaced; the *append-only-ledger + 5-state-machine + evidence-gated-done + dependency-holds* discipline IS the *charter §3.1 explicit-state-transition discipline + charter §3.2 StockEntry-append-only discipline by construction*).
- **Real observed LE31 JTBD**: none directly. The cross-section is *primitive vocabulary extension + append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern documentation*, not *LE31 demand*.
- **The value is primitive vocabulary extension + append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern documentation**: when (if) LE31 v1 or v2 extends the `StockEntry` append-only ledger with a state-machine discipline, an evidence-gated-done discipline, a dependency-holds discipline, or a Karpathy-wiki-pattern cross-session-memory primitive, the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern + zero-dependency-Python* vocabulary is documented.

## Description

GitHub `bangnevgo/ai-workflow-os` (0★/0⑂, MIT ✓, Python, pushed 2026-09-04T20:34:14Z + created 2026-09-04T19:51:03Z = both fields in-window; both ~30-min apart, 1119 KB substantial repo).

Description (verbatim): *"Append-only ledger + 5-state machine that keeps 6 divisions honest across 5 AI platforms (Claude, ChatGPT, Gemini). CLI for LLM agent workflow tracking: evidence-gated 'done', dependency holds, tamper-evident JSONL history, and Memory & Dreaming (Karpathy wiki pattern) cross-session memory for AI agents. Zero-dependency Python."*.

Topics (17, parent-verified count): `agent-memory`, `agent-orchestration`, `ai-agents`, `append-only`, `claude`, `claude-agents`, `developer-tools`, `jsonl`, `karpathy-wiki`, `ledger`, `llm`, `multi-agent`, `operating-system`, `python`, `state-machine`, `workflow`, `workflow-automation`.

The **7 named v1-append-only-ledger-state-machine primitives**:

| `bangnevgo/ai-workflow-os` primitive | LE31 v1 primitive | Match status |
|---|---|---|
| `Append-only ledger` (append-only-ledger) | (LE31 v1 `StockEntry` SQLModel = **charter §3.2 baseline match**) | **★ CHARTER-§3.2-BASELINE-MATCH** |
| `5-state machine` (5-state-machine) | (LE31 v1 `Order.status` SQLModel column tracks `pending → in-kitchen → ready → served → closed` = 5 states) | **★ PARTIAL MATCH: LE31 v1 has 5-state `Order.status`, but no 5-state-machine primitive on `StockEntry`** |
| `keeps 6 divisions honest across 5 AI platforms` (cross-platform-honesty) | (LE31 v1 has no cross-platform AI surface today) | **★ GAP: LE31 v1 has no cross-platform AI surface today** |
| `evidence-gated 'done'` (evidence-gated-done) | (LE31 v1 has no evidence-gated-done discipline today) | **★ GAP: LE31 v1 has no evidence-gated-done discipline today** |
| `dependency holds` (dependency-holds) | (LE31 v1 has no dependency-holds discipline today) | **★ GAP: LE31 v1 has no dependency-holds discipline today** |
| `tamper-evident JSONL history` (tamper-evident-JSONL-history) | (LE31 v1 `audit_logs` table = **partial match**, but no tamper-evident discipline) | **★ PARTIAL MATCH: LE31 v1 `audit_logs` exists, but no tamper-evident discipline** |
| `Karpathy wiki pattern cross-session memory` (Karpathy-wiki-pattern) | (LE31 v1 has no cross-session-memory primitive today) | **★ GAP: LE31 v1 has no cross-session-memory primitive today** |

The **append-only-ledger + 5-state-machine primitive**: the *append-only-ledger* + *5-state-machine* double IS the *charter §3.2 StockEntry-append-only + charter §3.1 explicit-state-transition discipline by construction* (every state transition writes a new row in the append-only ledger; every row is an event; every event is explicit — no silent transition; the entity's current state is derived from the rows; the ledger is the source of truth).

The **evidence-gated-done discipline**: an entity is not *done* until an evidence file is written; the *evidence-file* is the *evidence* that the work was done; the *evidence-gate* is the *gate* that prevents premature marking-as-done; the *evidence-gate* discipline IS the *charter §3.1 explicit-state-transition discipline applied to the done-state dimension* (an entity cannot move to *done* until an evidence file is written; the move requires explicit evidence; no silent transition).

The **dependency-holds discipline**: an entity cannot transition to *done* until all dependencies are satisfied; the *dependency* is the *prerequisite*; the *hold* is the *gate* that prevents premature transition when dependencies are unmet; the *dependency-holds* discipline IS the *charter §3.1 explicit-state-transition discipline applied to the dependency dimension* (an entity cannot move to a downstream state until all upstream dependencies are satisfied).

The **tamper-evident-JSONL-history primitive**: the *tamper-evident-JSONL-history* discipline IS the *charter §3.2 audit-log-integrity discipline by construction* (every event is written as a JSONL row; the *tamper-evident* qualifier = the history-cannot-be-tampered-with discipline; every row is hashed + chained; a tampered row breaks the hash chain; the chain is verifiable on demand).

The **Karpathy-wiki-pattern cross-session-memory primitive**: the *Karpathy-wiki-pattern* is Andrej Karpathy's *wiki* pattern for cross-session memory (the AI writes notes to its own wiki; the wiki is the cross-session memory; the *dreaming* pattern = periodically the AI reviews its own wiki + consolidates memories); the *Karpathy-wiki-pattern* discipline IS the *charter §3.4 observable-evidence discipline applied to the cross-session-memory dimension* (the wiki is the evidence; the wiki is observable; the AI cannot silently rewrite its own memory).

The **zero-dependency-Python posture**: the codebase uses only the Python standard library (no pip install needed; no requirements.txt); the *zero-dependency* posture matches LE31 v1's *lean-stack* posture (LE31 v1 uses FastAPI + SQLModel + aiogram v3 + Postgres — a minimal set of well-known dependencies).

## Data model

**No LE31 data model change today.** The defer artifact is documentation only. The *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern* primitive names the *append-only-ledger + state-machine + tamper-evident-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern* extensions that any future v1 or v2 schema change should consider; v1 or v2 maintainer decision required before adopting the append-only-ledger state-machine extension to `StockEntry` (LE31 v1 currently operates no state-machine extension to `StockEntry`; state-machine extension is a v1 or v2 architecture-decision, not a v1 implementation task today).

## Implementation steps

**NONE today.** The defer artifact is documentation only. Files to touch *if* the future v1 or v2 trigger condition fires (first v1 or v2 PR that extends the `StockEntry` append-only ledger with a state-machine discipline, an evidence-gated-done discipline, a dependency-holds discipline, or a Karpathy-wiki-pattern cross-session-memory primitive):

- `app/state_machine/` — possibly add (the state-machine module; depends on the v1 or v2 change).
- `app/state_machine/stockentry_machine.py` — possibly add (the state-machine that governs `StockEntry` transitions; depends on the v1 or v2 change).
- `app/state_machine/evidence_gated_done.py` — possibly add (the evidence-gated-done discipline; depends on the v1 or v2 change).
- `app/state_machine/dependency_holds.py` — possibly add (the dependency-holds discipline; depends on the v1 or v2 change).
- `app/audit_logs/tamper_evident.py` — possibly add (the tamper-evident discipline for `audit_logs`; depends on the v1 or v2 change).
- `app/cross_session_memory/karpathy_wiki.py` — possibly add (the Karpathy-wiki-pattern cross-session-memory primitive; depends on the v2 change).
- `tests/test_stockentry_machine.py` + `tests/test_evidence_gated_done.py` + `tests/test_dependency_holds.py` + `tests/test_tamper_evident.py` + `tests/test_karpathy_wiki.py` — possibly add (the integration tests for the state-machine + evidence-gated-done + dependency-holds + tamper-evident + Karpathy-wiki-pattern surfaces; depends on the v1 or v2 change).

**No existing file changes today.** No migration, no SQLModel schema change, no FastAPI route change, no Telegram handler change, no state-machine extension added.

## Telegram interaction if any

None today. If a future v2 trigger fires (state-machine extension to `StockEntry`), the Telegram interaction could be extended with state-machine-notifications: the cook gets a Telegram notification when a `StockEntry` transitions between states; the cook confirms the transition via the cook-bot; the state-machine discipline ensures the transition is legal and explicit. The *evidence-gated-done* discipline could be extended to require the cook to upload an evidence photo before the *done* state is reached; the *dependency-holds* discipline could be extended to require all upstream `StockEntry` events to be closed before the downstream event is marked *done*; the *Karpathy-wiki-pattern cross-session-memory* primitive could be extended to have the cook bot periodically review its own cross-shift memory + consolidate learnings.

## Dependencies

No new dependencies today. If the future v2 trigger fires, the dependencies would be:
- **None** — the codebase uses only the Python standard library (zero-dependency posture).

LE31 v1 currently operates no state-machine extension to `StockEntry`; the zero-dependency posture means no transitive dependency conflicts when the v1 or v2 surface is triggered.

## Open questions

1. **Does LE31 v1 or v2 ever extend the `StockEntry` append-only ledger with a state-machine discipline?** The cross-section value is the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern* vocabulary; v1 or v2 maintainer decision required before adopting the state-machine extension.
2. **Does LE31 v1 or v2 ever introduce an evidence-gated-done discipline?** The *evidence-gated-done* discipline IS the *charter §3.1 explicit-state-transition discipline applied to the done-state dimension*; v1 or v2 maintainer decision required before adopting the evidence-gated-done discipline.
3. **Does LE31 v1 or v2 ever introduce a dependency-holds discipline?** The *dependency-holds* discipline IS the *charter §3.1 explicit-state-transition discipline applied to the dependency dimension*; v1 or v2 maintainer decision required before adopting the dependency-holds discipline.
4. **Does LE31 v2 ever introduce a Karpathy-wiki-pattern cross-session-memory primitive?** The *Karpathy-wiki-pattern* discipline IS the *charter §3.4 observable-evidence discipline applied to the cross-session-memory dimension*; v2 maintainer decision required before adopting the Karpathy-wiki-pattern primitive.
5. **What triggers the v1 or v2 state-machine extension?** The trigger condition is the first v1 or v2 PR that extends the `StockEntry` append-only ledger with a state-machine discipline, an evidence-gated-done discipline, a dependency-holds discipline, or a Karpathy-wiki-pattern cross-session-memory primitive.

## Why this matters

The **append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python** primitive is the **strongest in-window 2026 sister-shape to feature 3 (`kitchen-stock-tracker` charter §3.2 baseline) the 54-pass brainstorm series has surfaced**. The cross-section value is the *append-only-ledger + 5-state-machine + evidence-gated-done + dependency-holds* discipline IS the *charter §3.1 + §3.2-by-construction* discipline (every state transition writes a new row in the append-only ledger; every row is an event; every event is explicit — no silent transition; the entity's current state is derived from the rows).

Without this artifact, the next v1 or v2 maintainer would need to independently discover the *append-only-ledger + state-machine + evidence-gated-done + dependency-holds* discipline from scratch; with this artifact, the next v1 or v2 maintainer has a *named* reference that documents the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python* vocabulary as the v1 append-only-ledger state-machine reference.

The MIT permissive license is §3.2 STRICTLY-COMPATIBLE — the codebase CAN be imported as a v2 dependency under MIT permissive licensing (no copyleft restrictions, no source-disclosure requirements); the zero-dependency posture means no transitive dependency conflicts; this gives LE31 v1 or v2 maintainers the option to *import* the *append-only-ledger + 5-state-machine + tamper-evident-JSONL-history + evidence-gated-done + dependency-holds + Karpathy-wiki-pattern cross-session-memory + zero-dependency-Python* primitives rather than re-implementing them.