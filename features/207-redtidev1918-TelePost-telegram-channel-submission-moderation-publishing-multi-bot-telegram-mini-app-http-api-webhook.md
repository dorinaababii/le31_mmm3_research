# Feature 207 — `redtidev1918-TelePost-telegram-channel-submission-moderation-publishing-multi-bot-telegram-mini-app-http-api-webhook` (defer)

> **NEW observation (2026-09-20).** Documents in-window GitHub Search `topic:telegram-bot` query result: `redtidev1918/TelePost` (**MIT ✓**, **16★/3⑂**, Python, **pushed 2026-09-20T02:18:09Z = TODAY** (in-window by `pushed_at` only — *created 2025-10-24T00:00:00Z = OUT-OF-WINDOW* = 11 months before the 30-day window start; **the repo is IN-WINDOW BY PUSH ONLY per the SKILL hard rule**), **2458 KB substantial repo**, default_branch=`main`). Topics (verbatim from raw JSON, **15 topics**): `automation`, `content-management`, `http-api`, `moderation`, **`multi-bot`**, `publishing`, `python`, `python-telegram-bot`, `self-hosted`, `submission`, **`telegram`**, **`telegram-bot`**, **`telegram-channel`**, **`telegram-mini-app`**, `webhook`. Description (verbatim, parent-verified GitHub API direct-GET 2026-09-20, translated from Chinese): *"Telegram channel submission, moderation, and automated publishing platform — supports Bot, Mini App, multi-bot, and HTTP API."* The **Telegram-multi-bot-channel-moderation operator-tooling primitive** + the **Telegram-channel-operations surface** + the **multi-bot + moderation + Mini-App + HTTP-API + webhook + self-hosted** sextuple-primitive: the *Telegram-channel-operations* surface pattern — when an owner runs a Telegram channel for their restaurant (a *menu channel* + an *order channel* + a *reservation channel* + a *review channel* + a *promo channel*), the multi-bot pattern coordinates the channels (each channel has its own bot; the bots coordinate via shared state). The *"multi-bot"* topic names the *multiple-bot-coordination* pattern. The *"telegram-mini-app"* topic names the *Mini-App integration* primitive (Mini Apps = Telegram's web-app-inside-chat surface; LE31 v1 has NO Mini App today; the Mini App is the *future v2 customer-facing Telegram surface* that the charter §3.4 *no customer-facing AI* posture would need to vet before adopting). The *"moderation"* topic names the *content-moderation* primitive (a Telegram channel with human reviewers + AI-assist moderation; **charter §3.4 explicitly aligned** — *moderation is operator-tooling, not customer-facing AI*; the moderation-bot is *staff-facing* in the §3.4 sense). The *"publishing"* + *"submission"* topics name the *content-pipeline* primitive (submission queue → human-review → moderation-decision → publish-to-channel; the *explicit-state-transitions* primitive applied to Telegram channel content). The *"http-api"* + *"webhook"* topics name the *HTTP-API + webhook-receiver* integration surface (LE31 v1's cook-bot today uses aiogram + Telegram long-polling, not HTTP-webhook; the HTTP-API primitive is the *future v2 web-facing surface*). The *"self-hosted"* topic is the **self-hosted-deployment primitive** (LE31 v1 is exactly self-hosted; the *self-hosted + python-telegram-bot* topic pair is the *LE31-stack-mirror*). **MIT ✓ §3.2 STRICTLY-COMPATIBLE** for vocabulary + architecture-reference adoption; **§3.4 explicitly aligned** (the moderation-bot is operator-tooling, not customer-facing AI; the Mini App component would require future §3.4 review before adoption). The **16★/3⑂ community adoption signal** is meaningful — the 16★ community has validated the *Telegram-multi-bot-channel-moderation* primitive pattern at scale. Bucket: **v2 Telegram-operator (Telegram-channel-operations + multi-bot + moderation + Mini-App + HTTP-API + webhook + self-hosted primitive, parking-lot defer)** — vocabulary + Telegram-channel-operations primitive + multi-bot + moderation + Mini-App + HTTP-API + webhook + self-hosted documentation, zero build time today.

## Goal

Retain the **"Telegram channel submission, moderation, and automated publishing platform — supports Bot, Mini App, multi-bot, and HTTP API"** cross-section architectural vocabulary + **Telegram-multi-bot-channel-moderation operator-tooling primitive** + **multi-bot + moderation + Mini-App + HTTP-API + webhook + self-hosted** sextuple as the persistent v2 Telegram-operator reference for any future LE31 v2 maintainer asking the *Telegram-channel-coordination* question (does LE31 v2 ever need a Telegram-channel-bot for restaurant operations? does LE31 v2 ever need multi-bot-coordination across multiple Telegram bots? does LE31 v2 ever need a moderation-bot with human-review-queue? does LE31 v2 ever need a Mini App surface for guest-facing order placement?). The artifact is the persistent *Telegram-channel-operations primitive + multi-bot + moderation + Mini-App + HTTP-API + webhook + self-hosted* vocabulary as a *named* v2 architectural reference. No code today.

## Scope

**In scope (defer artifact):**
- A written record of the `redtidev1918/TelePost` cross-section vocabulary: the **first in-window 2026 Python Telegram-multi-bot-channel-moderation platform repo of the 44-pass brainstorm series** with the *telegram + telegram-bot + telegram-channel + telegram-mini-app + multi-bot + publishing + moderation + http-api + webhook + submission + self-hosted + automation + content-management + python + python-telegram-bot* 15-topic set; the **15-topic Telegram-channel-operations surface coverage is the broadest observed in the 44-pass series** (vs the *8-topic* `nematjon555/telegram-restaurant-delivery-bot` from feature 117 = 1.9× fewer topics; vs the *6-topic* `SGrappelli/pronto` from feature 100 = 2.5× fewer topics).
- A written record of the **Telegram-multi-bot-channel-moderation operator-tooling primitive**: the *Telegram-channel-operations* surface pattern — when an owner runs a Telegram channel for their restaurant (a *menu channel* + an *order channel* + a *reservation channel* + a *review channel* + a *promo channel*), the multi-bot pattern coordinates the channels (each channel has its own bot; the bots coordinate via shared state). The *multi-bot coordination* discipline is the **multi-bot-orchestration primitive** — a single bot cannot serve 5 channels with different roles; multi-bot is the *bot-as-microservice* pattern.
- A written record of the **content-moderation primitive**: a Telegram channel with human reviewers + AI-assist moderation; the *moderation-bot* receives content submissions, applies AI-assist moderation (e.g. profanity-filter + spam-detection), and either approves or rejects; a human reviewer makes the final decision on borderline content; the *content-moderation* discipline is the **explicit-state-transitions primitive applied to Telegram channel content** (submission → moderation-pending → moderation-approved → publish-to-channel, OR submission → moderation-pending → moderation-rejected → notify-author).
- A written record of the **Mini-App integration primitive**: Mini Apps = Telegram's web-app-inside-chat surface (a web-app that opens inside a Telegram chat, with access to Telegram user-data + Telegram WebApp API); the *Mini-App* pattern is the **future v2 customer-facing Telegram surface** that the charter §3.4 *no customer-facing AI* posture would need to vet before adopting; LE31 v1 has NO Mini App today; the Mini App could host the *guest-facing menu + order placement* surface.
- A written record of the **HTTP-API + webhook primitive**: the *HTTP-API* is the *external HTTP-endpoint for content-submission + status-check* surface; the *webhook* is the *inbound HTTP-callback for Telegram events* surface (vs LE31 v1's aiogram + Telegram long-polling, which polls Telegram for events); the *HTTP-API + webhook* pair is the *future v2 web-facing surface* that LE31 v1's Telegram bot does NOT use today.
- A written record of the **self-hosted + python-telegram-bot LE31-stack-mirror primitive**: the *self-hosted + python-telegram-bot* topic pair is the *LE31 v1 backend-mirror* (LE31 v1 is self-hosted + Python + aiogram v3 + Telegram long-polling); TelePost is self-hosted + Python + python-telegram-bot + HTTP-webhook; the *self-hosted + Python + python-telegram-bot* triple is the *LE31 v1 backend-stack-mirror* (LE31 v1 uses aiogram v3 instead of python-telegram-bot, but both are Python Telegram bot libraries).
- A written record of the **MIT license as a documented strictly-adoptable artifact**: the codebase CAN be adopted as a reference for the *Telegram-multi-bot-channel-moderation pattern* (MIT = §3.2 STRICTLY-COMPATIBLE for vocabulary AND architecture-reference adoption); LE31 would re-implement the multi-bot-channel-moderation pattern against its own FastAPI + SQLModel + aiogram v3 stack, not import the python-telegram-bot code (LE31 v1 uses aiogram v3, not python-telegram-bot; both are Python Telegram bot libraries but with different APIs).
- A decision record: today's verdict is `defer (parking-lot)` because the *Telegram-multi-bot-channel-moderation + Mini-App + HTTP-API + webhook + self-hosted* vocabulary is a v2 Telegram-operator reference, not a v1 or v2 build implication. v1 has a single Telegram bot for the cook channel today; the multi-bot-coordination + moderation + Mini-App + HTTP-API + webhook vocabulary documents the *future-extension* for the next v2 maintainer asking the *Telegram-channel-coordination* question.

**Out of scope (defer artifact):**
- Any change to the LE31 v1 Telegram bot (no multi-bot-coordination today, no moderation-bot today, no Mini App today, no HTTP-webhook today).
- Any change to the LE31 v1 waiter web UI (no Telegram-channel-bot today, no submission-pipeline today, no publishing-pipeline today).
- Adoption of the `redtidev1918/TelePost` code as a v1/v2 dependency (the repo is 16★/3⑂ at 2458 KB; the *Telegram-multi-bot-channel-moderation + Mini-App + HTTP-API + webhook + self-hosted* vocabulary would need to be independently re-implemented and validated against the LE31 v1's aiogram v3 + Telegram long-polling + single-bot posture, not simply imported; the python-telegram-bot library is off-pattern for LE31 v1's aiogram v3 stack).
- Any change to the `audit_logs` table or `StockEntry` ledger today.
- Any new dependency on the `redtidev1918` maintainer.

## Evidence / JTBD

When a future LE31 v2 maintainer asks *"if v2 introduces a Telegram-channel-bot for restaurant operations, multi-bot-coordination across multiple Telegram bots, a moderation-bot with human-review-queue, or a Mini App surface for guest-facing order placement, what is the Telegram-multi-bot-channel-moderation operator-tooling primitive that preserves the existing v1 single-bot + aiogram v3 + Telegram long-polling + no-Mini-App posture?"*, the maintainer wants *evidence that another independent 2026 Python repo at 2458 KB with MIT license + in-window-by-push-only + 16★/3⑂ + 15 topics including telegram + telegram-bot + telegram-channel + telegram-mini-app + multi-bot + publishing + moderation + http-api + webhook + submission + self-hosted + automation + content-management + python + python-telegram-bot is shipping the Telegram-channel-operations primitive as the v2 operator-tooling discipline*, but struggles because *v1 has no documented Telegram-channel-operations primitive in the charter*, so that *v2 can introduce the Telegram-channel-coordination + multi-bot + moderation + Mini-App + HTTP-API + webhook vocabulary with the explicit channel-operations discipline rather than reinventing the pattern*.

- **Evidence class**: observed (the `redtidev1918/TelePost` description explicitly names *"Telegram channel submission, moderation, and automated publishing platform"* = the Telegram-channel-operations primitive; the *multi-bot + telegram-mini-app + moderation + http-api + webhook + submission + publishing* topic set names the *multi-bot-coordination + Mini-App + content-moderation + HTTP-API + webhook + content-pipeline* primitive set).
- **Confidence**: medium-high for the Telegram-multi-bot-channel-moderation + Mini-App + HTTP-API + webhook + self-hosted vocabulary (MIT + Python + 2458 KB substantial + in-window-by-push-only + 16★/3⑂ + 15 topics + explicit Telegram-channel-operations description + multi-bot + telegram-mini-app + moderation + http-api + webhook + submission + publishing + self-hosted = §3.2 STRICTLY-COMPATIBLE + §3.4 explicitly aligned for moderation-bot + Mini-App requires future §3.4 review).
- **Real observed LE31 JTBD**: none directly. The cross-section is *primitive vocabulary extension + Telegram-channel-operations documentation*, not *LE31 demand*.
- **The value is primitive vocabulary extension + Telegram-channel-operations documentation**: when (if) LE31 v2 introduces a Telegram-channel-bot for restaurant operations, multi-bot-coordination, a moderation-bot, or a Mini App surface, the *Telegram-multi-bot-channel-moderation + Mini-App + HTTP-API + webhook + self-hosted* vocabulary is documented.

## Description

GitHub `redtidev1918/TelePost` (MIT, 16★/3⑂, Python, pushed 2026-09-20T02:18:09Z = in-window by `pushed_at` only; created 2025-10-24T00:00:00Z = out-of-window, 2458 KB substantial repo).

Description (verbatim, translated from Chinese): *"Telegram channel submission, moderation, and automated publishing platform — supports Bot, Mini App, multi-bot, and HTTP API."*

Topics (verbatim from raw JSON, 15 topics): `automation`, `content-management`, `http-api`, `moderation`, `multi-bot`, `publishing`, `python`, `python-telegram-bot`, `self-hosted`, `submission`, `telegram`, `telegram-bot`, `telegram-channel`, `telegram-mini-app`, `webhook`.

The **6 named Telegram-channel-operations primitives**:

| redtidev1918/TelePost primitive | LE31 v1 primitive | Match status |
|---|---|---|
| `multi-bot` | (LE31 v1 has a single Telegram bot for the cook channel today; no multi-bot-coordination) | **★ GAP: LE31 v1 has no multi-bot-coordination primitive today** |
| `telegram-mini-app` | (LE31 v1 has no Mini App today; no guest-facing Telegram surface) | **★ GAP: LE31 v1 has no Mini App primitive today** |
| `moderation` | (LE31 v1 has no moderation-bot today; no content-review-queue today) | **★ GAP: LE31 v1 has no moderation primitive today** |
| `http-api` + `webhook` | (LE31 v1 uses aiogram v3 + Telegram long-polling today; no HTTP-API + no webhook) | **★ GAP: LE31 v1 has no HTTP-API + no webhook primitive today** |
| `submission` + `publishing` | (LE31 v1 has no content-pipeline today; the cook-bot receives orders from the waiter web UI, but no external submission-pipeline) | **★ GAP: LE31 v1 has no submission + publishing primitive today** |
| `self-hosted` + `python-telegram-bot` | LE31 v1 is self-hosted + Python + aiogram v3 (python-telegram-bot is a different library, but the *self-hosted + Python* pair is on-pattern) | ✓ partial match |

The **Telegram-multi-bot-channel-moderation operator-tooling primitive**: the *Telegram-channel-operations* surface pattern — when an owner runs a Telegram channel for their restaurant (a *menu channel* + an *order channel* + a *reservation channel* + a *review channel* + a *promo channel*), the multi-bot pattern coordinates the channels (each channel has its own bot; the bots coordinate via shared state). The *multi-bot coordination* discipline is the **multi-bot-orchestration primitive** — a single bot cannot serve 5 channels with different roles; multi-bot is the *bot-as-microservice* pattern.

The **content-moderation primitive**: a Telegram channel with human reviewers + AI-assist moderation; the *moderation-bot* receives content submissions, applies AI-assist moderation (e.g. profanity-filter + spam-detection), and either approves or rejects; a human reviewer makes the final decision on borderline content; the *content-moderation* discipline is the **explicit-state-transitions primitive applied to Telegram channel content** (submission → moderation-pending → moderation-approved → publish-to-channel, OR submission → moderation-pending → moderation-rejected → notify-author). **Charter §3.4 explicitly aligned** — *moderation is operator-tooling, not customer-facing AI*; the moderation-bot is *staff-facing* in the §3.4 sense.

The **Mini-App integration primitive**: Mini Apps = Telegram's web-app-inside-chat surface (a web-app that opens inside a Telegram chat, with access to Telegram user-data + Telegram WebApp API); the *Mini-App* pattern is the **future v2 customer-facing Telegram surface** that the charter §3.4 *no customer-facing AI* posture would need to vet before adopting; LE31 v1 has NO Mini App today; the Mini App could host the *guest-facing menu + order placement* surface. **Charter §3.4 future-review required** — the Mini App is a *customer-facing Telegram surface* and would need explicit owner-decision before adoption.

The **HTTP-API + webhook primitive**: the *HTTP-API* is the *external HTTP-endpoint for content-submission + status-check* surface; the *webhook* is the *inbound HTTP-callback for Telegram events* surface (vs LE31 v1's aiogram v3 + Telegram long-polling, which polls Telegram for events); the *HTTP-API + webhook* pair is the *future v2 web-facing surface* that LE31 v1's Telegram bot does NOT use today.

The **self-hosted + python-telegram-bot LE31-stack-mirror primitive**: the *self-hosted + python-telegram-bot* topic pair is the *LE31 v1 backend-mirror* (LE31 v1 is self-hosted + Python + aiogram v3 + Telegram long-polling); TelePost is self-hosted + Python + python-telegram-bot + HTTP-webhook; the *self-hosted + Python + python-telegram-bot* triple is the *LE31 v1 backend-stack-mirror* (LE31 v1 uses aiogram v3 instead of python-telegram-bot, but both are Python Telegram bot libraries with similar capabilities).

The **MIT license as a documented strictly-adoptable artifact**: the codebase CAN be adopted as a reference for the *Telegram-multi-bot-channel-moderation pattern* (MIT = §3.2 STRICTLY-COMPATIBLE for vocabulary AND architecture-reference adoption); LE31 would re-implement the multi-bot-channel-moderation pattern against its own FastAPI + SQLModel + aiogram v3 stack, not import the python-telegram-bot code (LE31 v1 uses aiogram v3, not python-telegram-bot; both are Python Telegram bot libraries but with different APIs).

## Data model

**No LE31 data model change today.** The defer artifact is documentation only. The Telegram-multi-bot-channel-moderation operator-tooling primitive names the *multi-bot + moderation + Mini-App + HTTP-API + webhook + submission + publishing + self-hosted* extensions that any future v2 schema change should consider; v2 maintainer decision required before adopting the multi-bot-channel-moderation pattern (LE31 v1 currently operates a single Telegram bot for the cook channel; multi-bot-coordination is a v2 architecture-decision, not a v1 implementation task).

## Implementation steps

**NONE today.** The defer artifact is documentation only. Files to touch *if* the future v2 trigger condition fires (first v2 PR that adds a Telegram-channel-bot to the LE31 operator surface, a multi-bot coordination pattern across multiple Telegram bots, a moderation-bot with human-review-queue, or a Mini App surface for guest-facing order placement):

- `app/bot/channels/` — possibly add (the multi-bot-coordination directory; each channel gets its own bot module; depends on the v2 change).
- `app/bot/moderation.py` — possibly add (the moderation-bot that applies AI-assist moderation + human-review; depends on the v2 change).
- `app/api/telegram_webhook.py` — possibly add (the HTTP-webhook endpoint for inbound Telegram events; replaces the aiogram v3 long-polling; depends on the v2 change).
- `app/api/telegram_http_api.py` — possibly add (the HTTP-API endpoint for outbound Telegram content-submission + status-check; depends on the v2 change).
- `app/web/mini_app.py` — possibly add (the Mini App web-app surface for guest-facing menu + order placement; depends on the v2 change; **charter §3.4 future-review required**).
- `tests/test_multi_bot.py` + `tests/test_moderation.py` + `tests/test_telegram_webhook.py` + `tests/test_telegram_http_api.py` + `tests/test_mini_app.py` — possibly add (the integration tests for the multi-bot-coordination + moderation + HTTP-webhook + HTTP-API + Mini App surfaces; depends on the v2 change).

**No existing file changes today.** No migration, no SQLModel schema change, no FastAPI route change, no Telegram handler change, no Mini App added.

## Telegram interaction if any

None today. If a future v2 trigger fires (multi-bot-coordination pattern), the Telegram interaction could be extended to multiple bots: (a) a cook-bot for the cook channel (existing); (b) a wait-bot for the waiter channel; (c) a reservation-bot for the reservation channel; (d) a review-bot for the review channel; (e) a promo-bot for the promo channel; (f) a moderation-bot for content-moderation across all channels. The bots coordinate via shared state in the database (e.g. a `bot_state` table with `bot_name` + `last_message_id` + `last_update_at`). If the future v2 trigger fires (Mini App surface), the Telegram interaction could be extended to a guest-facing Mini App that opens inside a Telegram chat, displays the menu, accepts orders, and shows order-status. **Charter §3.4 future-review required** for any Mini App surface.

## Dependencies

- No new dependency today.
- If a future v2 trigger fires:
  - Software: a Python Telegram bot library (LE31 v1 uses aiogram v3; v2 could also use python-telegram-bot OR add FastAPI webhook support to the existing aiogram v3 stack; depends on the v2 change).
  - Software: a content-moderation library (e.g. `profanity-filter` for profanity detection + `scikit-learn` for spam-classification; LE31 currently has no moderation dependency).
  - Software: a Mini App framework (e.g. Telegram WebApp API + React or HTMX; LE31 currently has no Mini App dependency).
  - Schema: a `bot_state` table for multi-bot-coordination + a `moderation_queue` table for content-moderation-pending; depends on the v2 change.

## Open questions

- Should LE31 v2 introduce a Telegram-channel-bot for restaurant operations (e.g. menu channel + order channel + reservation channel)?
- Should LE31 v2 introduce multi-bot-coordination across multiple Telegram bots?
- Should LE31 v2 introduce a moderation-bot with human-review-queue (charter §3.4 explicitly aligned)?
- Should LE31 v2 introduce a Mini App surface for guest-facing order placement (charter §3.4 future-review required)?
- Should LE31 v2 introduce HTTP-API + webhook endpoints for the Telegram bot surface (replacing the aiogram v3 long-polling)?
- If yes to any of the above: should the v2 PR cross-reference this feature 207 contract + HANDOFF as the named architectural reference?

## Why this matters

The *Telegram-multi-bot-channel-moderation + Mini-App + HTTP-API + webhook + self-hosted* primitive is the **gap** between LE31 v1's single-Telegram-bot + aiogram v3 + Telegram long-polling + no-Mini-App posture and a *multi-bot-coordinated + moderation-enabled + Mini-App-extended + HTTP-API-driven* v2 Telegram-operator surface. For LE31 v2's restaurant-operations surface (multi-channel Telegram-coordination + content-moderation + guest-facing Mini App + HTTP-API + webhook), the TelePost pattern is the *future-extension reference*: when (if) LE31 v2 introduces a Telegram-channel-bot for restaurant operations, multi-bot-coordination, a moderation-bot, or a Mini App surface, the *Telegram-multi-bot-channel-moderation + Mini-App + HTTP-API + webhook + self-hosted* vocabulary is documented.
